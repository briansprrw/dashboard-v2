# M0.1 — Source Reconciliation

**Milestone:** M0 — Product and Architecture Decisions
**Work packet:** M0.1 — Source reconciliation
**Author:** Claude (implementation lead)
**Date:** 2026-07-23
**Status:** Ready for PM/QA
**Reviewer:** Codex

This document is the conflict list required by M0.1. It compares every planning input, labels each statement, and points to the resolution that M0.3 must apply. It **does not change scope**: where the approved decision record ([M0-D1..D24](./M0-approved-decisions-2026-07-23.md)) already resolves a conflict, this document records the resolution; it does not re-open it.

## Sources compared

| Key | Source | Authority level |
|---|---|---|
| **AR** | [M0 Approved Decision Record — 2026-07-23](./M0-approved-decisions-2026-07-23.md) | **Approved (Brian).** Overrides all others. |
| **DL** | M0 Decision Log (M0-D1..D24) | Approved (Brian); mirrors AR, including the same-day privacy, scope, and guided-review revisions. |
| **PP** | [Product plan](../plans/2026-07-22-dash2-product-plan.md) | Proposal. |
| **TA** | [Technical architecture](../plans/2026-07-22-dash2-technical-architecture.md) | Proposal. |
| **IP** | [Implementation plan](../plans/2026-07-22-dash2-implementation-plan.md) | Proposal. |
| **MK** | [Coded mockup](../mockups/dash2.html) | Visual proposal; not approved by existing. |
| **CX** | [Codex defensive review](../audits/2026-07-22-codex-review.md) | Historical V1 review (reference). |
| **FB** | [Claude Fable 5 review](../audits/2026-07-22-claude-fable-review.md) | Historical V1 review (reference). |
| **RR** | [Rewrite-vs-revamp memo](../audits/2026-07-22-rewrite-vs-revamp.md) | Historical recommendation (superseded on the rewrite question). |

Label meanings (per M0.1 instructions):

- **Approved** — settled by AR/DL; canonical.
- **Proposed** — in a plan/mockup but not yet approved; may still need an M0.3 acceptance ID.
- **Historical** — describes V1 or a superseded recommendation; reference only, never Dash2 scope.
- **Conflicting** — two live sources disagree and the disagreement is not yet reconciled in the canonical plans (even when AR settles the *decision*, the plan text still contradicts it until M0.3 edits it).

---

## Part 1 — Terminology conflicts

| # | Statement | Sources | Label | Resolution for M0.3 |
|---|---|---|---|---|
| T1 | Canonical user-facing term is **List**. | AR §2, DL M0-D2 | Approved | — |
| T2 | "Keep **Sheet** for migration familiarity." (P1) | PP P1, PP throughout (`sheets`, `sheet_memberships`, feature IDs) | **Conflicting** vs T1 | Rewrite PP user-facing "Sheet" → "List". **Keep** storage/table identifiers (`sheets`, `sheet_memberships`, `owner_user_id`) as-is — AR §9 approves those exact identifiers; "List" is the product term, not a table rename. Note the split explicitly so M1 does not rename tables. |
| T3 | Concept glossary defines "Sheet … may be renamed to **List** during product review." | PP "Core concepts" | Proposed → resolved | Update glossary: List is the term; Sheet is historical/migration-only. |
| T4 | Term **recycle bin** (not "archive") for ordinary deletion. | AR §4, DL M0-D5 | Approved | PP/TA/IP say "archive"/"soft-delete" (T-archive rows below). Reconcile vocabulary to "recycle bin" for the user-facing lifecycle; "soft delete" remains an acceptable internal/storage description. |

---

## Part 2 — Feature-priority conflicts (PP §Feature inventory)

Brian's entered "Your priority" overrides win (AR §1, DL M0-D1). Remaining `Unrated` inherit the **Recommended priority**. Conflicts are where Brian's entry differs from the recommendation, or where an AR scope decision overrides an inherited priority.

| # | Feature | Recommended | Brian's entry | Label | Resolution |
|---|---|---|---|---|---|
| F-A11 | Theme presets | Nice to Have | **Important** | Approved override | Launch = Important. Themes = Dark/Darker/Light/High Contrast (AR §7). |
| F-A12 | Custom colors and emoji | Important | *(Unrated)* + note "Freeform emojis, but colors shifted into Themes" | Approved override (note = requirement) | Emoji freeform per-device; custom color goes through **Themes** (validated), not per-emoji color. |
| F-B14 | Search | Important | **Nice to have** | Approved override | Search is **post-launch** (Nice to Have). Removes it from Critical/Important launch set. |
| F-B15 | Sort per sheet | Important | **Nice to have** + note | Approved override (note = requirement) | Manual/selectable sort is post-launch. **Launch default sort is fixed** and required: *Urgent first always, then by date, then by importance, then alphabetically.* |
| F-B16 | Filters | Important | **Nice to have** | Approved override | Filters post-launch. (Mockup shows filter chips — MK conflict, see V-row.) |
| F-B17 | Manual reordering | Nice to Have | Nice to have | Approved (agree) | Post-launch. `sort_key` seam retained (TA). |
| F-B18 | Bulk actions | Nice to Have | **Not important** | Approved override | Excluded. |
| F-B19 | Recurring tasks | Nice to Have | **Not important** | Approved override | Excluded (also post-launch candidate PP). |
| F-B20 | Subtasks | Not Important | *(Unrated)* | Inherit | Excluded. |
| F-B21 | Tags | Nice to Have | **Not Important** | Approved override | Excluded. |
| F-B22 | Attachments | Not Important | *(Unrated)* | Inherit | Excluded. |
| F-C7 | Multiple dashboards | Nice to Have | **Not important** | Approved override + AR §2 | Deferred to V2.1 (one dashboard at launch). |
| F-C8 | Dashboard sharing | Nice to Have | **Not important** | Approved override | Excluded/deferred. |
| F-C9 | Sheet templates | Not Important | Not important | Approved (agree) | Excluded. |
| F-D7 | Additional identity providers | Not Important | **Nice to have** | Approved override | Still not launch; deferred (AR §11 lists MS/Apple as future). Net: **deferred**, not launch. |
| F-E3.5 | Private tasks | (Priority: Important) | Important | Approved | In launch (AR §1, §3). |
| F-E5 | Notes private by default | Critical | *(Unrated)* + note | Inherit + note | Notes visible to editors/viewers **unless** marked private (AR §3 private note). Public: notes **never** public (AR §6). |

**Whole-section overrides from AR that beat inherited priorities:**

| # | Statement | Sources | Label | Resolution |
|---|---|---|---|---|
| P-E1 | Public dashboards / public username (E1–E11) recommended mostly Critical/Important. | PP §E | **Conflicting** vs AR | AR §2/§6, DL M0-D7 **defer all public output to V2.1** + permanent site-wide `noindex`. Public-output E-features are **Deferred (V2.1)** regardless of priority. E3.5 (private tasks) and E8 (permanent noindex enforcement) are V2 exceptions because they protect authenticated/future surfaces without launching public output. |
| P-D3 | Invite codes "Important". | PP D3 | **Conflicting** vs AR | AR §2: "V2 supports only users migrated from V1. V2 does not create new accounts through invite codes." → Invite issuance/redemption **excluded** at launch. Architecture may keep an invite seam but launch has no onboarding. |
| P-A13/A14/A15 | Multiple dashboards / kiosk link / auto-rotation. | PP §A | Proposed → deferred | AR §2/§8/§11 defer smart-frame sessions and multi-dashboard to V2.1. Smart-frame **sized layout** must still render at launch (AR §7). |

---

## Part 3 — Roles / permissions / privacy conflicts

| # | Statement | Sources | Label | Resolution |
|---|---|---|---|---|
| R1 | Roles: Viewer / Editor / Owner / Admin. | AR §3, PP §Roles | Approved | Canonical matrix authored in M0.3 (acceptance M0-E2). |
| R2 | "Editor … **Proposed:** can archive tasks." | PP Editor | Proposed → resolved | AR §3/§4: Editor **may recycle** a task; **cannot restore or permanently delete**. Remove "Proposed" hedge. |
| R3 | Editor may move a task between sheets "only when they are an editor or owner of both." | PP Editor, AR §3, TA `canMoveTask` | Approved (agree) | — |
| R4 | Owner permanently deletes tasks "according to the approved retention policy." | PP Owner | Proposed → resolved | AR §4: only **List owner** restores/permanently-deletes an individual recycled task; 30-day window; early purge with confirmation. |
| R5 | Private tasks/notes are List-owner-only through the application; hidden from Viewer, Editor, and Admin. | AR §3, DL M0-D16 (supersedes the Admin-visibility portion of M0-D4) | Approved | M0.3 must enforce the owner-only read boundary in the matrix and acceptance tests. |
| R6 | Admin administrative/recovery God Mode is separate from content visibility: Admin may operate by opaque identity but cannot read private tasks, private notes, or task-history field values. Cryptographic protection is V2.1. | AR §3/§5, DL M0-D16 | Approved | Add explicit query/DTO/history/audit denial paths; do not create a temporary V2 Admin content exception. |
| R7 | "A user deletion must transfer or archive owned sheets before the user is removed." | PP Rules-requiring-approval | **Conflicting** vs AR | AR §4 (Accounts): account recycle **soft-deletes the account together with every List and task it owns**; owned Lists disappear for other members until restore. So V2 does **not** force ownership transfer before removal — it recycles the whole owned unit. Reconcile PP rule to the AR account-recycle model. Ownership transfer still exists as a separate owner/admin action. |
| R8 | "Every sheet must have exactly one valid owner." | PP, TA design rules, CLAUDE.md | Approved (agree) | Invariant. Reconcile with R7: a recycled account's Lists are owned-but-hidden, not ownerless. |
| R9 | New shares default to Viewer. | AR §3, PP C-rules, TA | Approved (agree) | — |
| R10 | Legacy `can_see=1` migration → editor (P7). | PP P7, IP mapping, RR, CX, FB | **Conflicting** vs AR | AR §3, DL M0-D3/D13: **V1 sharing is NOT migrated.** Users re-share deliberately. Override P7 and the IP `access_control → sheet_memberships` mapping row: shares are **dropped**, not converted to editor. |

---

## Part 4 — Lifecycle conflicts (deletion / retention / archive)

| # | Statement | Sources | Label | Resolution |
|---|---|---|---|---|
| L1 | 30-day recycle window for tasks, Lists, accounts; auto-purge after. | AR §4, DL M0-D5 | Approved | — |
| L2 | P3 "Keep [closed] indefinitely … archive after a configurable period." | PP P3 | **Conflicting** vs L1 | Closed-task *visibility* (hide/N-days/always, complete & cancelled separate — PP F5 note, MK) is distinct from the recycle lifecycle. Reconcile: closed tasks stay live (visibility-controlled); **recycling** is the 30-day delete path. Remove "keep indefinitely + archive" ambiguity. |
| L3 | P4 sheet deletion "Soft-delete for 30 days before permanent purge." | PP P4 | Proposed → matches L1 | Confirm as List recycle (folder semantics, AR §4). |
| L4 | P5 task deletion "Archive first; permanent deletion from the archive." | PP P5 | Proposed → resolved | Rename archive→recycle bin; Editor recycles, Owner restores/purges. |
| L5 | TA `sheets.state CHECK ('active','archived')`, `tasks.archived_at`. | TA schema | Proposed (storage) | Storage may keep `archived`/`archived_at` naming internally, but the **product lifecycle is recycle-bin**. M0.3/M2 should either rename to `recycled_at` or document the mapping so the vocabulary is unambiguous. Recommend `recycled_at` for clarity; flag as an M2 schema decision, non-blocking for M0. |
| L6 | Account removal = admin-controlled recycle; cascades to owned Lists/tasks; suspends memberships. | AR §4 | Approved | Not in PP/TA. Add to lifecycle section + schema notes (needs `users.state` to include a recycled/soft-deleted state beyond `active`/`disabled` — flag for M2). |
| L7 | Task history survives recycle/restore; purged on permanent delete. | AR §4/§5 | Approved | TA `task_events` exists; add retention/purge semantics. |

---

## Part 5 — Architecture conflicts

| # | Statement | Sources | Label | Resolution |
|---|---|---|---|---|
| A1 | Greenfield, independent React/Vite + authored CSS on one Hono Worker; own D1/KV/OAuth/host/deploy. | AR §9, DL M0-D11, TA | Approved | TA "Architectural decisions to approve" table is all `TBD` → M0.3 marks them **Approved** per AR. |
| A2 | **Rewrite-vs-revamp memo recommends "rearchitect, do NOT restart from zero"** and preserving V1 frontend/DB incrementally. | RR (whole doc), CX exec verdict, FB exec verdict | **Historical / superseded** | AR §9 + DL M0-D11/D14 chose **greenfield**. RR/CX/FB "don't rewrite" conclusion is **superseded**; their *findings about V1 debt* remain useful migration/reference inputs. M0.3 need not edit the audits (they are historical), but must not treat "don't rewrite" as live guidance. |
| A3 | `dash2.dnky.us` initial host; cut `dash.dnky.us` over on approval. | AR §9 | Approved | PP/TA say `dash2.dnky.us`; consistent. CLAUDE.md mission still says V1 "at `dash.dnky.us`" is source of truth — consistent (that's the cutover target). |
| A4 | `/api/v1` private internal, runtime-validated, explicit DTOs; not a public API. | AR §9, DL M0-D12, TA | Approved | TA "supported third-party API" language: ensure PP D-features / E-features don't imply a public API at launch (they don't after E deferral). |
| A5 | UUID IDs; `owner_user_id` canonical; Viewer/Editor membership rows; reviewed SQL (no ORM); KV sessions + short-lived state only. | AR §9, DL M0-D12, TA | Approved | — |
| A6 | Migration: read-only V1; no write freeze required; authorized wipe-and-recopy before stability; final = fresh copy + reconcile. | AR §10, DL M0-D13/D14 | Approved | **Conflicts** with IP/TA "short **V1 write freeze**" (below). |
| A7 | IP guiding rule #7 + TA "Cutover consistency": "final migration uses a **short, announced write freeze**." | IP §Guiding rules, IP Phase 8, TA §Migration | **Conflicting** vs A6 | AR §10 + DL M0-D14: **no write freeze required** (Brian is the only active V1 user and coordinates directly). M0.3 must change IP/TA freeze language to "coordinated single-user pause, no formal write freeze." M8 launch procedure "Announce the V1 write-freeze" → reword. |
| A8 | Migrate users/roles, Lists/ownership, all feasible task data; exclude shares, settings, invite codes, legacy config. | AR §10, DL M0-D13 | Approved | IP migration mapping table converts `access_control`→memberships and `settings`→preferences and `invite_codes`→invites. **Override**: shares, settings, invites are **not** migrated. Update IP mapping table rows accordingly. |
| A9 | `noindex` site-wide, always. | AR §6, DL M0-D7 | Approved | **Conflicts** with the original PP E8 "Owner chooses whether indexing is allowed" and TA `public_profiles.allow_indexing`. Resolution: replace E8 with permanent `noindex` enforcement, remove `allow_indexing`, and never expose an indexing opt-in in V2 or V2.1. |

---

## Part 6 — Layout / device / theme conflicts

| # | Statement | Sources | Label | Resolution |
|---|---|---|---|---|
| V1 | Automatic 1–3 column responsive flow through 1920×1080; no manual column selector; desktop/phone/tablet. | AR §7, DL M0-D8, MK (`repeat(auto-fit, minmax(300px,1fr))`) | Approved | **Conflicts** with PP A9 / P14 "user-selectable one- or two-column." Remove the manual selector; layout is automatic. Mockup's Display-profile "Two-column layout on wide screens" toggle is a **conflict** (MK) — resolve toward automatic flow, or reframe the toggle as "allow multi-column" cap, not a fixed count. Recommend: automatic only. |
| V2 | Narrow acceptance widths **420×1080 and 640×1080**. | AR §7 | Approved | PP/IP say "narrow desktop column at primary real-world width and 1080 height" without the two exact widths; MK narrow frame = 420px. Adopt 420 and 640 as the two narrow reference widths. |
| V3 | Smart-frame **sized** layout renders at launch; dedicated frame auth/profiles = V2.1. | AR §7/§8, DL M0-D7 | Approved | PP smart-frame/kiosk features (A14/A15) deferred; sized rendering (1280×800, 1920×1080 no-hover) stays in the test matrix. |
| V4 | Zoom: **7 steps, −3..+3, 10%/step.** | AR §7, MK ("−3 to +3, 10% per step") | Approved | PP "adjustable scale" (A6) unquantified; adopt the 7-step spec. |
| V5 | Themes: **Dark** (V1 solid rows), **Darker** (translucent, mockup style), **Light**, **High Contrast**; custom via validated controls; no arbitrary CSS. | AR §7, DL M0-D9, MK (Style A solid / Style B translucent) | Approved | PP A11 "Dark default plus high-contrast and light" omits Darker and the solid/translucent distinction. Mockup's two task styles map to Dark (A/solid) vs Darker (B/translucent). Adopt 4 built-ins. |
| V6 | Status/priority meanings semantic; emoji customizable per device; theme supplies defaults. | AR §7, DL M0-D9, MK (`STATUS_ICON`, `PRIORITY_ICON`) | Approved | — |
| V7 | Portrait desktop/monitor + ultrawide layouts = future. | AR §7/§11, DL M0-D8 | Approved | Remove from launch device matrix. |
| V8 | Device display prefs stored **locally per device**; not synced; no private content in local storage. | AR §7/§8, DL M0-D9/D10, TA state table | Approved | TA `device_profiles` server table is a deferred seam; launch uses local storage. Consistent. |

---

## Part 7 — Refresh / offline / session conflicts

| # | Statement | Sources | Label | Resolution |
|---|---|---|---|---|
| S1 | Refresh 60s default, 10s min, 10m max. | AR §8, DL M0-D10, PP F4 note, MK ("10s min · 10m max") | Approved (agree across sources) | — |
| S2 | Offline: keep in-memory data, show `Offline`, disable edits; no private content persisted for reload. | AR §8, DL M0-D10, PP P15, TA refresh behavior | Approved (agree) | PP P15 "show last successful in-memory data with a timestamp; no offline writes" is consistent. |
| S3 | Sessions: **30-day sliding**; immediate server revocation on disable/recycle. | AR §8, DL M0-D10, PP D4, TA session record | Approved (agree) | TA mentions "absolute maximum if approved" — not approved; launch = sliding 30-day, revocable, no separate absolute cap unless later approved. |
| S4 | Dedicated long-lived read-only display sessions = V2.1. | AR §8, DL M0-D7 | Approved | PP A14 / P13 smart-frame session → deferred. |

---

## Part 8 — Data-shape / enum facts (for M0.4 + M2), reconciled from mockup + audits

These are **facts to carry forward**, not conflicts, unless noted. Source of workflow enums is the mockup (which mirrors V1 `CONFIG`), cross-checked against PP.

| # | Fact | Sources | Label | Note |
|---|---|---|---|---|
| E1 | **Status enum:** not started, in progress, pending, blocked, complete, cancelled. | MK `STATUS_ICON`, PP B6 | Approved-consistent | Canonical launch status set. |
| E2 | **Priority enum:** low, medium, high, urgent. | MK `PRIORITY_ICON`, PP B7 | Approved-consistent | `complete` icon (🎉) is a *derived display* state, not a stored priority. |
| E3 | **Due-color bands:** overdue / today / soon (1–3d) / soonish (4–7d) / future (8+d) / complete / unscheduled. | MK `dueColorClass`, PP task-display spec | Proposed → adopt | PP lists "overdue, today, soon, future, completed, unscheduled" (6); MK adds **soonish** (7). Reconcile to the 7-band mockup model, or record the 6-band PP model — **recommend 7 bands** (mockup is the visual target). Flag as an M3 visual decision; non-blocking for M0. |
| E4 | Default sort: **Urgent first, then date, then importance, then alpha.** | PP B15 note (Brian) | Approved | Launch-fixed sort (F-B15 above). |
| E5 | Default status emoji: 🗓️ ⏳ ⏰ ⛔️ ✅ ✖️; priority 💤 🟢 ⚠️ ☢️ (+🎉 complete). | MK | Proposed defaults | Theme-supplied defaults; user-overridable per device (V6). |
| E6 | Closed-task config: **complete and cancelled are separate** hide/N-days/always controls. | PP F5 note (Brian), MK | Approved | — |
| E7 | Task row grammar: `[status][name][note][emoji flags][due][priority]`. | MK `taskRowHtml`, PP task-display spec | Proposed → adopt | Consistent; note MK places note-dot + emoji between name and due. |
| E8 | V1 sheet slugs are **email-derived** (`email.replace('@','_')`); `MAX(row_index)+1` ordering; migrations 0001–0006; `can_see` single permission bit. | CX C1/C8, FB F1/F13, RR §2 | **Historical (V1)** | Migration-input facts only. Confirms: do NOT carry email-derived slugs into V2 public IDs (AR §6/§9 UUIDs). Any live re-profiling is confined to M6 and requires Brian's separate authorization. |

---

## Part 9 — Items with NO conflict but MISSING an acceptance ID (for M0.3)

These are approved or clearly in-scope but lack a testable acceptance criterion in PP/IP. M0.3 must add IDs.

- Private task / private note visibility matrix (all audiences), including explicit Admin denial and opaque recovery behavior.
- Recycle-bin 30-day lifecycle for each of task / List / account, incl. cascade + restore-as-unit.
- Task-history field visibility (owner only), allowlisted Admin recovery/audit metadata, and purge-on-permanent-delete.
- Administrative audit event stream + allowlisted metadata.
- Fixed launch sort order.
- 7-step zoom; 4 built-in themes; per-device emoji.
- Automatic 1–3 column flow; 420/640 narrow widths; smart-frame sized rendering.
- 60/10/600s refresh bounds; Offline behavior; 30-day sliding sessions + immediate revocation.
- Site-wide `noindex`.
- Greenfield isolation invariants (separate Worker/D1/KV/OAuth/host).
- Migration accounting: every source row imported/transformed/omitted/rejected.

---

## Summary of overrides M0.3 must apply to the canonical plans

1. **Sheet → List** in all user-facing PP text; keep table identifiers.
2. **Archive → recycle bin** vocabulary; 30-day lifecycle for task/List/account.
3. **Defer all public/E-features + username + smart-frame sessions + multi-dashboard + invites/onboarding to V2.1**; unconditional site-wide `noindex`.
4. **Do not migrate** V1 shares, settings, or invite codes (override P7 + IP mapping rows).
5. **No V1 write freeze** (override IP #7, IP Phase 8, TA cutover) → single-user coordinated pause.
6. **Automatic 1–3 column** layout (override PP A9/P14 manual selector).
7. **4 built-in themes** (Dark/Darker/Light/High Contrast); 7-step zoom; per-device emoji.
8. Mark all TA architecture-decision-table rows and PP P1–P15 **Approved/Deferred** per AR.
9. Add the missing acceptance IDs listed in Part 9.
10. Adopt reconciled enums/bands (Part 8), flagging the 7-band due-color and `recycled_at` naming as M2/M3-level confirmations (non-blocking for M0 exit).
