# M2 — Domain, Authentication, and Authorization

**Status:** **Accepted (Brian, 2026-07-27).** M2.1–M2.4 are committed (`78e1484`); the full correction wave on top — M2-R12/R13/R14 (Claude self-review), M2-QA-01 through M2-QA-05 (Codex independent QA round 1), M2-AR-01/02/03 (M2.5 adversarial review), M2-AR-01-FOLLOWUP (task-move ownership-confirmation gate), M2-R3's restore rehearsal, M2-FQA-01 through M2-FQA-06 (Codex's full-QA pass), M2-FQA-RR-01 through M2-FQA-RR-03 (Codex's re-review), M2-FQA-RR2-01 (Codex's second re-review), and M2-FQA-RR3-01 (Codex's third re-review, documentation wording only) — is accepted with it. **Not yet committed to Git** (see Decision Log/PM-QA sign-off for commit timing). **The isolated OAuth callback and staging-environment prerequisite is resolved** (M2-D15, M2-R4). **M2-D12, M2-D14, and the M2.5 model deviation are all confirmed as specified** (M2-D18). **M2-R9's governance half is confirmed authorized** (M2-D18). **Accepted with one item explicitly carried forward, not closed:** the live Google callback/token-exchange leg (M2-R7) and applying migration `0004_identity_subject_pending.sql` to the real `dash2-preview` database are both deferred to a later work wave, by Brian's explicit choice — see the final PM/QA sign-off below. **Schema note:** the domain schema is four migration files (`0001`–`0004`). `EXPECTED_SCHEMA_VERSION` is `3`. `0001` matches `78e1484` exactly. `0002` and `0003` do **not** match `78e1484` — `0002` still carries the uncommitted M2-D16 trigger-split edits, and `0003` is itself an uncommitted, untracked file — but both are the shape already live on `dash2-preview` (M2-R9). `0004` has **not** been applied to any remote database and requires its own `production-mutation-gate` authorization, deliberately deferred alongside M2-R7. Five consecutive independent Codex review rounds each found and closed real gaps in the previous round's own fix, on authorization, private-content write-scoping, and migration-delivery-safety axes — see M2-D19 through M2-D22 and the M2-FQA/M2-FQA-RR/M2-FQA-RR2/M2-FQA-RR3 Risk Log rows below. All resolved and independently re-verified except the two items explicitly accepted as deferred.\
**Owner:** Brian  
**Implementation lead:** Claude  
**Primary model:** Claude Opus 5, `xhigh` effort (the milestone index is authoritative; the earlier "Opus 4.8" naming here predates the 2026-07-24 routing update in `docs/milestones/README.md`)\
**Review model:** Separate Claude Opus 5, `xhigh`, fresh context\
**PM/QA:** Codex  
**Estimated focused time:** 5–7 days  
**Production impact:** Isolated staging API only. **Nothing in M2.1 touched any deployed resource** — all migration and repository evidence is from the local Miniflare D1 simulation.

## Outcome

Deliver the stable server-side domain foundation: schema, repositories, services, Google OAuth/session lifecycle, and centrally enforced permissions for users, Lists, memberships, and tasks.

## Prerequisites

- M1 is Accepted.
- M0 role/action/visibility and lifecycle decisions are recorded.
- Isolated staging OAuth callback, D1, and KV resources are available.

## In scope

- Migrations for users, identities, auth/session support, Lists, memberships, tasks, preferences needed by core flows, task/audit events required by approved policy, and schema versioning.
- Explicit constraints, indexes, timestamps, `recycled_at` fields/`recycled` account state, and approved identifiers.
- Repository and domain-service boundaries.
- Google OAuth with high-entropy, expiring, one-time state.
- Opaque server-side sessions, secure cookie policy, sliding/absolute expiry, logout, user/auth-version revocation, and active-user recheck.
- Owner/editor/viewer/admin policies, with administrative authority separated from private task/note/history visibility.
- Runtime schemas, explicit DTOs, origin/CSRF defenses, bounded inputs, parameterized SQL, stable error codes.
- Profile bootstrap fields limited to Google-provided display name/avatar and browser-derived locale/timezone; no V2 profile editor/public username.
- Unit, repository/integration, contract, and authorization-matrix tests.

## Out of scope

- Production OAuth/DNS or live-user onboarding.
- Complete frontend dashboard.
- Management/admin UI.
- Anonymous public routes.
- Invite issuance, redemption, and management (V2.1).
- V1 migration importer.
- Optional search, bulk actions, recurrence, subtasks, tags, or attachments.

## Invariants to prove

- Every sheet has exactly one active owner.
- Owner membership/ownership cannot be removed without an atomic approved transfer.
- A disabled/deleted user cannot use an existing session.
- Role and auth-version changes take effect without waiting for cookie expiry.
- A task always references a valid sheet.
- Moving a task requires the approved rights in source and destination.
- Private tasks/notes obey the approved matrix on reads and writes: List owner may read; Viewer, Editor, and Admin may not. Admin recovery actions operate by opaque identity and do not return protected fields.
- Admin overrides create required audit evidence.
- External DTOs never expose database rows wholesale.

## Work packets

### M2.1 — Schema and repositories — **Implemented 2026-07-24, Ready for PM/QA**

Implement migrations with constraints/indexes and repositories that return domain records/DTO inputs rather than leaking raw rows through routes. Test actual SQL against a migrated database.

Delivered: `migrations/0002_domain_schema.sql` (8 domain tables, CHECK constraints, per-relationship FK delete semantics, 14 indexes, 3 ownership-invariant triggers), `src/shared/domain/` records + enums + bounds, 7 repositories under `src/server/repositories/`, and a new `integration` Vitest project that runs the real migration files against a real D1 database inside workerd (112 tests). See M2-E1 and M2-D2..M2-D5.

### M2.2 — Policy/service layer — **Implemented 2026-07-25, Ready for PM/QA**

Implement centralized policy functions and invariant-preserving services. Route handlers must not duplicate or bypass authorization policy.

Delivered: `src/server/policy/` (6 modules — `actor`, `sheet-access`, `content-visibility`, `admin-policy`, `authorization-error`, `index`) as pure, I/O-free decision functions transcribing Launch Contract §2; `src/server/services/` (6 modules — `sheet-service`, `task-service`, `admin-recovery-service`, `account-service`, `audit`, `service-context`) which load facts, ask policy, then mutate. Content visibility is a separate axis from authority, so `isAdmin` never grants a protected read. See M2-E2 and M2-D7..M2-D10.

### M2.3 — OAuth and sessions — **Implemented 2026-07-25, Partial (M2-R4)**

Implement initiation, state storage/consumption, callback, eligibility, session creation/refresh/revocation, logout, cookie policy, and generic user-facing auth errors. Never log tokens, codes, state, cookies, or provider payloads.

Delivered: `src/server/auth/` (8 modules) — one-time/expiring/server-bound OAuth state and opaque sessions in KV (both stored **hashed**), PKCE S256, 30-day sliding + 90-day absolute expiry, `auth_version` revocation recheck on every request, HttpOnly/SameSite=Lax/env-driven-Secure cookies, and uniform generic auth failures. The Google adapter sits behind an `IdentityProviderClient` seam so the one unverifiable dependency is isolated to a single file. **Not verified:** the live Google round-trip (M2-R4). See M2-E3, M2-D11..M2-D13.

### M2.4 — Contracts and protected-content boundaries — **Implemented 2026-07-25, Ready for PM/QA**

Implement runtime validation and explicit DTO boundaries. Prove that Admin recovery can act on opaque identifiers without returning private task, note, or history-field values.

Delivered: `src/shared/contracts/` (`validation`, `requests`, `dto`) — bounded, closed (unknown-field-rejecting) runtime validation producing one consistent `400 VALIDATION_FAILED` envelope, and allowlist-constructed DTOs; `src/server/middleware/origin.ts` and `authenticate.ts`; `src/server/routes/{auth,sheets,tasks,admin}.ts`. The administrative surface returns only recovery DTOs built from the M2.1 allowlisted projections. See M2-E4 and M2-D14.

### M2.5 — Adversarial review

In a fresh Opus context, review authorization bypasses, confused-deputy paths, ownerless states, IDOR, session fixation/replay, CSRF/origin behavior, enumeration, and sensitive error/log content. Claude implementing the feature may not be the only reviewer.

## Acceptance criteria

Legend: `[x]` = implementation evidence produced by Claude, pending independent Codex verification; `[~]` = partially satisfied by a completed packet, remainder owned by a later packet; `[ ]` = not started.

- [x] Every migration applies to an empty database and the resulting constraints/indexes match the approved model. — M2-E1. `npm run db:migrate:local` applied `0001` + `0002` from a deleted `.wrangler/state` **twice**, plus an idempotent re-apply ("No migrations to apply!") in between; applied objects audited via `wrangler d1 execute` (10 tables, 3 triggers, 14 `idx_*` indexes, `schema_version = 2`) and asserted by exact-list tests in `test/integration/schema.test.ts`.
- [x] Repository integration tests execute the real SQL for create/read/update/recycle/restore/transfer sequences. — M2-E1. 112 tests in the new `integration` Vitest project run inside workerd against a real Miniflare-backed D1 with the actual migration files applied by `applyD1Migrations`, not a mock.
- [x] The complete approved role/action matrix has allow and deny contract tests. — M2-E2. `test/unit/authorization-matrix.test.ts` transcribes Launch Contract §2 as a table and asserts **every** capability against **every** role column (Viewer, Editor, Owner, Admin, stranger, disabled, recycled, disabled-admin, disabled-owner), so a widened permission fails a test naming the role it was widened to. 112 assertions there, plus 49 service-level allow/deny tests against real D1 in `test/integration/authorization-service.test.ts`.
- [x] Viewers receive `403` for every mutation; unauthenticated callers receive the approved `401` behavior. — M2-E2. Seven distinct viewer mutations (task create/update/recycle, List rename/recycle, membership grant, ownership transfer) each asserted `403`, with a positive control proving the viewer can still read. All 21 protected route/method pairs asserted `401` unauthenticated in `test/contract/authenticated-routes.test.ts`, including a regression test that an unresolvable session yields `401` rather than `503` when OAuth is unconfigured.
- [x] Disabled/deleted users and revoked auth versions lose access immediately. — M2-E3. Asserted against real KV/D1 with **no clock advance**: an `auth_version` bump, a `disable`, and a `recycle` each cause the very next `resolveSession` to fail, and the KV record is destroyed rather than merely refused so restoring the state cannot resurrect the session. A deleted user's session is also rejected. Role changes bump `auth_version` in the same service method as the state change.
- [x] Owners cannot orphan a sheet through membership changes, ownership transfer, or user deletion. — M2-E1 (storage) + M2-E2 (service). The M2.1 backstops stand; M2.2 adds explicit service enforcement so the API cannot reach those states: granting the owner a membership is `409`, transferring to a disabled or recycled account is `409`, transferring to the current owner is `409`, and a transfer runs membership removal + owner change in one D1 batch. Tested end to end, including that a denied transfer leaves exactly one owner and no residual membership, and that deleting a List-owning user is still refused.
- [x] Invalid IDs, enums, dates, lengths, unknown sensitive fields, origin, and content types fail consistently. — M2-E4. `src/shared/contracts/validation.ts` produces one `400 VALIDATION_FAILED` envelope with per-field messages for every case; 47 tests in `test/unit/contracts.test.ts` cover shape, enum, UUID, length-boundary (at max and max+1), unknown-field rejection, and content type. Notably `2026-02-30` is rejected here — it passes both the shape regex and the database GLOB, so this layer is the only one that catches it. Origin enforcement is asserted separately in the contract tests.
- [~] OAuth state is one-time, expiring, bound to server-held context, and safe against replay **within a single Cloudflare location; the cross-location case is an accepted residual risk, not verified as impossible.** — M2-E3. Against **real Miniflare KV** (a single-location simulation): 256-bit state, stored under a SHA-256 hash (asserted absent from every KV key name), consumed exactly once (a replay returns null), expired past its 10-minute TTL, and *spent even when expired* so a late attempt cannot succeed either. The PKCE verifier and post-login redirect live only in the KV record, never the URL. A replayed callback is refused **without any provider call**, asserted via an exchange counter. Redirect paths are sanitised against open redirect (absolute, protocol-relative, backslash, and `javascript:` forms). **Codex M2-QA-02, resolved by Brian's risk acceptance (M2-QA-02-DECISION):** Workers KV is only eventually consistent across Cloudflare locations (up to 60s propagation), so two callbacks racing the same state from different locations within that window could theoretically both succeed — Miniflare's single-location test cannot exercise or rule this out. Brian approved accepting this narrow, documented gap rather than adding new strongly-consistent infrastructure.
- [x] Session cookies meet the approved Secure/HttpOnly/SameSite/Path policy. — M2-E3. `HttpOnly`, `SameSite=Lax`, `Path=/`, `Max-Age` matching the sliding window, and `Secure` driven by `COOKIE_SECURE` — which yields `Secure` unless explicitly set to `"false"`, so the insecure setting must be chosen deliberately and is never reached by omission (AC-D2's "explicit env-driven Secure"). The clearing cookie repeats every attribute so the browser actually replaces it.
- [~] Private task/note reads and mutations match the M0 matrix, including direct Admin API denial and opaque Admin recovery responses that contain no protected task, note, or history fields. — M2-E4 + M2-E5 + M2-E7 + **M2-E8**. **Incorrectly claimed three times**, each time on stronger evidence than the last. First (before M2.5): held on the *administrative* surface only, failed on the *ordinary* surface via ownership acquisition (M2-AR-01, P1, fixed). Second (Codex's full-QA pass, 2026-07-26): the ownership-acquisition fix did not address *creation or transition into* private state — an Editor or Admin could create a task with `isPrivate: true`, or flip an ordinary task to private (**M2-FQA-03**, P1, fixed), and a full task replacement (PUT) that omitted `isPrivate`/`notesPrivate` silently declassified existing content (**M2-FQA-02**, P1, fixed). Third (Codex's re-review of that fix, same day): `canWriteTaskAsPrivate` covered `isPrivate` but left `notesPrivate` as an independent, unguarded axis — a non-owner could still write or edit a private note on a task that itself stayed public (**M2-FQA-RR-02**, P1, fixed by `canWriteTaskNotesAsPrivate`); separately, a confirmed relinquishing move stopped returning a false 404 but still returned full task fields to an actor `canReadTask` denied, redacting only the note text (**M2-FQA-RR-03**, P2, fixed by an opaque move-result DTO). Marked `[~]` rather than `[x]`: three independent passes have now each found a real hole in this exact criterion, most recently one *in the immediately preceding pass's own fix*, so a fourth pass finding a fourth gap cannot be ruled out from this record alone. This fourth correction is Claude self-verified, not yet independently re-verified by Codex. Policy, DTO, and denial-shape evidence from the first three passes stands unchanged and was not reopened, except where explicitly superseded above.
- [x] Profile/bootstrap contracts expose Google display name/avatar and browser locale/timezone without a profile-edit/public-username mutation surface. — M2-E3. `parseProfileBootstrap` accepts **only** `locale` and `timezone`; `displayName`, `avatarUrl`, and `username` are rejected as unknown fields (asserted). Display name and avatar are provider-sourced and refreshed only on sign-in. `toSessionUserDto` omits `authVersion` and email.
- [x] Logs/errors/audit records exclude secrets and unapproved private content. — M2-E2/M2-E3. Audit metadata is typed to primitives only and serialised by one helper, with marker-scan tests proving a List name and a task name/note never reach an audit row. Auth failure reasons go to the server log and never to the response; every user-facing auth failure is identical. The provider adapter never reads or logs a token-endpoint error body (it echoes the authorization code). Repository-wide scan for client secrets, API keys, private keys, and real emails: clean.
- [~] Fresh adversarial review has no open P0/P1. — M2-E5. **Performed 2026-07-25.** One P1 found (M2-AR-01, ownership acquisition defeats the protected-content boundary), reproduced against real D1, fixed at the policy layer, and pinned by 25 regression tests. **0 open P0/P1 remain.** Marked `[~]` rather than `[x]` for two reasons a reviewer must not skip: (1) the review ran on **Claude Fable 5**, not the milestone's specified separate **Opus 5 `xhigh`** context — Brian explicitly overrode the model for this run, recorded as a deliberate choice rather than a substitution; (2) "no open P0/P1" means none found by this pass, which is the identical claim every prior pass in this milestone made before an external check found something. Two P3 observations recorded (M2-AR-02, M2-AR-03).

## Required evidence

- Schema diagram and migration/check output.
- Machine-readable or tabular authorization test matrix.
- OAuth/session sequence with test IDs, not secrets.
- Protected-content DTO and opaque-recovery contract results.
- Adversarial review findings and dispositions.
- API error examples with synthetic values.

## QA approach

Codex independently tests horizontal and vertical privilege changes, stale sessions, direct API calls with hidden UI ignored, source/destination task moves, owner transfer failure injection, and private-field access. Tests should prefer generated users/IDs and synthetic content.

The review session uses Opus 4.8 `xhigh`. `max` is allowed only for a specific unresolved invariant/race after the normal review produces conflicting evidence.

## Rollback

Redeploy the prior isolated staging Worker and restore/recreate the staging D1 database. Database rollback must follow the tested restore procedure; Worker rollback alone is insufficient after schema changes.

## Evidence Index

| ID | Acceptance criterion | Evidence | Result | Reviewer |
|---|---|---|---|---|
| M2-E1 | Schema/repository correctness | **M2.1, 2026-07-24.** `migrations/0002_domain_schema.sql` (8 domain tables, per-relationship FK delete semantics, 14 indexes incl. 5 partial, 3 ownership triggers); `EXPECTED_SCHEMA_VERSION` 1→2; `src/shared/domain/{enums,records,limits}.ts`; 7 repositories under `src/server/repositories/`. New `integration` Vitest project (`vitest.workers.config.ts`, `@cloudflare/vitest-pool-workers` 0.18.8) runs the **real migration files** against a **real D1** inside workerd via `applyD1Migrations`. Checks run in-worktree with observed exit results: `format:check` PASS, `lint` PASS, `typecheck` PASS, `test` PASS (135 = 23 node + 112 integration), `db:migrate:local` from deleted `.wrangler/state` PASS ×2 with an idempotent re-apply in between, `build` PASS, `git diff --check` exit 0. Applied-object audit via `wrangler d1 execute --local`: 10 tables / 3 triggers / 14 `idx_*` / `schema_version = 2`. Real `wrangler dev --local` run confirms `GET /api/v1/health` → `200` `{"status":"ok","schemaVersion":2,"expectedSchemaVersion":2}` and the 404 envelope still intact | Produced; Codex verification pending | Codex |
| M2-E2 | Authorization matrix | **M2.2, 2026-07-25.** `src/server/policy/` (6 pure modules) + `src/server/services/` (6 modules). Machine-readable matrix: `test/unit/authorization-matrix.test.ts` asserts every Launch Contract §2 capability against all 9 role/state columns (112 assertions), including a sweep proving ineligible accounts lose *every* capability — which catches a future capability added without an eligibility check. Service-level enforcement against real D1: `test/integration/authorization-service.test.ts` (49 tests) covering viewer-denied mutations, editor recycle-but-not-restore asymmetry, stranger 404-not-403, both-Lists move rule (including that a denied move leaves the task in place), ownership-invariant conflicts, recycle-before-purge, and admin account administration with `auth_version` bumps. Checks run in-worktree: `format:check` PASS, `lint` PASS, `typecheck` PASS, `test` PASS (446), `build` PASS, `git diff --check` exit 0 | Produced; Codex verification pending | Codex |
| M2-E3 | OAuth/session lifecycle | **M2.3, 2026-07-25 — PARTIAL.** `src/server/auth/` (8 modules, now including `rate-limit.ts` from the M2-QA-04 correction). `test/integration/auth-lifecycle.test.ts` runs against **real Miniflare KV + real D1**: state entropy/hashing/one-time-consumption/expiry/replay, PKCE, open-redirect sanitisation, session sliding + absolute ceiling, refresh threshold, logout, tampered tokens, immediate revocation via `auth_version`/`disable`/`recycle` with no clock advance (now atomic per-statement-batch, M2-QA-03), and a bounded redirect path + rate limiter (M2-QA-04). Cookie policy asserted in `test/contract/authenticated-routes.test.ts`. **The OAuth client, redirect URI, and preview-as-staging environment now exist** (M2-D15, M2-R4 resolved) — a live read-only `GET /api/v1/auth/start` reaches Google's real authorization endpoint. **Still NOT RUN — the live Google callback/token-exchange leg**: `google-provider.ts` is structurally correct and its authorization-request half is verified live, but the token exchange and tokeninfo call are unexercised because they require a human completing a real sign-in in a browser, which Claude cannot do (M2-R7). **Resolved (Codex M2-QA-02):** OAuth state's one-time consumption and session logout's immediacy are implemented as KV get-then-delete/put-then-delete; Workers KV is only eventually consistent, so the code's "atomic"/"immediate" comments overstated what KV alone guarantees under real cross-location traffic. Brian approved accepting this as a documented, bounded residual risk (M2-QA-02-DECISION) rather than adding new infrastructure; the code comments are corrected accordingly | Partial; live provider callback unverified (M2-R7) | Codex + Brian (M2-R7) |
| M2-E4 | Protected DTO/recovery boundaries | **M2.1 repository half, 2026-07-24.** `test/integration/protected-content.test.ts` (24 tests): the three administrative recovery/metadata reads are built from allowlisted SQL column lists that never select `name`, `notes`, `is_private`, `notes_private`, `status`, `priority`, `due_date`, the List `display_name`, or `changes_json`. Asserted with exact `Object.keys` equality, per-field `Object.hasOwn` checks, and a `JSON.stringify` scan for a synthetic private marker; a positive control confirms the owner history read still returns the full values, so the separation is a real distinction rather than a uniformly broken read. Purging a recycled private task using only its opaque id is exercised end to end **M2.2/M2.4 completion, 2026-07-25.** The policy and DTO halves now exist. `test/integration/admin-recovery-boundary.test.ts` (15 tests) proves the full stack: an Admin restoring or purging a **private** task with private notes receives only `{id, sheetId, recycledAt, createdAt, updatedAt}` — asserted by exact `Object.keys`, per-field `Object.hasOwn`, and `JSON.stringify` marker scans — while positive controls confirm the owner still receives the name, notes, and privacy flag, and the List name. `src/shared/contracts/dto.ts` builds every response from named fields, and the administrative DTOs are structurally distinct types (not a task DTO with fields omitted), so a handler cannot return one where the other is expected. Runtime validation evidence: 47 tests in `test/unit/contracts.test.ts` | Complete across repository, policy, and DTO layers; Codex verification pending | Codex |
| M2-E5 | Adversarial review | **M2.5, 2026-07-25 — performed on Claude Fable 5 (`claude-fable-5`) at Brian's explicit override, not the specified separate Opus 5 `xhigh` context.** Fresh session: this context did not implement M2.1–M2.4. Attacked authorization bypasses, confused-deputy paths, ownerless states, IDOR, session fixation/replay, CSRF/origin ordering, enumeration, and error/log content across `policy/`, `services/`, `auth/`, `middleware/`, `routes/`, `http/`, `contracts/`, and `observability/`. **One P1 found: M2-AR-01** — content visibility derives from `ownerUserId === actor.userId`, and both sides were writable, so an Admin could transfer any List to themselves and an Admin *or any Editor* could move a task into a List they own, then read the private note and owner-only history through the ordinary owner surface, where none of the three administrative barriers applies. Reproduced against **real migrated D1 in workerd** by a temporary proof that asserted the *bad* outcome and passed three ways pre-fix (note via transfer, note via move, history via transfer) plus the Editor variant; those proofs were deleted and replaced by denial tests. Fixed at the policy layer: `canAssignOwnershipTo` refuses naming yourself owner of a List you do not own, and `canMoveTask` refuses an ownership-acquiring move. Pre-fix, 4 of the 14 new integration tests failed while the 4 legitimate-path controls passed, so the tests discriminate rather than pass uniformly. Checks after the fix: `format:check` PASS, `lint` PASS, `typecheck` PASS, `test` PASS (**508**, was 484), `build` PASS, `db:migrate:local` PASS ("No migrations to apply!"), `git diff --check` exit 0, secret scan clean. Two P3 observations recorded: M2-AR-02 (admin task move writes no audit row) and M2-AR-03 (`exp`/`iss` not checked explicitly in `verifyIdToken`) | Complete; 0 open P0/P1; independently unverified; model deviation recorded | Codex |
| M2-E6 | First-pass self-review and correction (not M2.5) | **2026-07-25, Claude Sonnet 5 (deviation from the milestone's specified Opus 5 `xhigh` review, recorded per Brian's explicit instruction).** Read-through of the full policy/service/auth/route/DTO surface against the Launch Contract §2 matrix and the milestone's own flagged risks. Found and fixed 3 issues (M2-R12 P2, M2-R13/R14 P3); no P0/P1 found. Checks after fixes: `format:check` PASS, `lint` PASS, `typecheck` PASS, `test` PASS (464, was 462), `build` PASS, `git diff --check` exit 0. This is explicitly **not** a substitute for M2.5 (fresh-context adversarial review) or Codex QA — both remain open | Complete; independently unverified | Codex |
| M2-E7 | Full-QA correction (M2-FQA-01 through M2-FQA-06) | **2026-07-26, Claude Sonnet 5 (session model, deviation recorded per M2-D18).** Corrects all 6 findings from Codex's full-QA pass (4 P1, 2 P2): M2-FQA-01 (migrated-user identity rebind, `src/server/auth/auth-service.ts` + `UserRepository.rebindProviderSubject`), M2-FQA-02 (privacy flags required not defaulted, `src/shared/contracts/requests.ts`), M2-FQA-03 (`canWriteTaskAsPrivate` authorizes the requested state on create/update, `src/server/policy/content-visibility.ts` + `task-service.ts`), M2-FQA-04 (cross-repository D1 batching via new `prepareX()` statement builders and a shared `db` handle on `ServiceDeps`, applied to every mutation with required history/audit evidence across `task-service.ts`, `sheet-service.ts`, `account-service.ts`, `admin-recovery-service.ts`), M2-FQA-05 (admin restore appends the `restored` history event, `admin-recovery-service.ts`), M2-FQA-06 (`TaskService.move` returns the destination context so the route does not re-authorize the moved task as the former owner, `task-service.ts` + `routes/tasks.ts`). 18 new/updated regression tests, including 2 real D1 failure-injection tests proving batch atomicity (a rejected second statement leaves the first statement's mutation unapplied). Checks: `format:check` PASS, `lint` PASS, `typecheck` PASS, `test` PASS (**535**, was 517), `build` PASS, `git diff --check` exit 0, secret scan of touched files clean | Complete; Codex re-verification pending | Codex |
| M2-E8 | Re-review correction (M2-FQA-RR-01 through M2-FQA-RR-03) | **2026-07-26, same session as M2-E7.** Corrects all 3 findings from Codex's re-review of the M2-E7 fixes (2 P1, 1 P2): M2-FQA-RR-01 (permanent one-way identity binding — schema addition `user_identities.subject_pending`, guarded rebind in `UserRepository.rebindProviderSubject`, refuse-not-rebind logic in `auth-service.ts`), M2-FQA-RR-02 (`canWriteTaskNotesAsPrivate` closes the independent `notesPrivate` authorization gap, `content-visibility.ts` + `task-service.ts`), M2-FQA-RR-03 (opaque `TaskMoveResultDto` replaces a full task DTO whenever `canReadTask` denies the actor post-move, `dto.ts` + `routes/tasks.ts`). 16 new/updated regression tests, including 3 for M2-FQA-RR-01's permanent-binding boundary and a route-equivalent marker-scan test for M2-FQA-RR-03. Checks: `format:check` PASS, `lint` PASS, `typecheck` PASS, `test` PASS (551, was 535), `build` PASS, `git diff --check` exit 0, secret scan clean. **The M2-FQA-RR-01 schema delivery mechanism in this pass was wrong — see M2-E9** | Application/policy logic complete; schema delivery corrected in M2-E9; Codex re-verification pending | Codex |
| M2-E9 | Migration delivery correction (M2-FQA-RR2-01) | **2026-07-27.** Corrects the one finding from Codex's second re-review: `subject_pending` was being delivered by editing the already-shipped `0002` migration in place, which would never reach the real `dash2-preview` database. Reverted `migrations/0002_domain_schema.sql` to the shape it held immediately before this correction touched it — the already-applied, post-trigger-split content, with only the `subject_pending` edit removed (that content differs from committed `78e1484`; see M2-FQA-RR3-01). Added `migrations/0004_identity_subject_pending.sql` (append-only `ALTER TABLE ... ADD COLUMN`); bumped `EXPECTED_SCHEMA_VERSION` 2 → 3; corrected two runbooks' hardcoded "schema version 2" references. Verified against a fresh empty local D1 (4 migrations apply cleanly, idempotent re-apply confirms nothing pending) and against a simulated upgrade from the `dash2-preview`-equivalent pre-`0004` state (migrations `0001`–`0003` applied and tracked in `d1_migrations`, then `migrations apply` correctly identified and applied only `0004`). Checks: `format:check` PASS, `lint` PASS, `typecheck` PASS, `test` PASS (**552**, was 551), `build` PASS, `git diff --check` exit 0 | Complete; independently re-verified by Codex (M2-FQA-RR2-01 closed) with one P2 documentation-wording finding, M2-FQA-RR3-01 | Codex |
| M2-E10 | Documentation-wording correction (M2-FQA-RR3-01) | **2026-07-27.** Corrects the one P2 finding from Codex's third re-review: the M2-E9/M2-D21 record described `0002`'s restoration as "its exact prior committed shape," which is imprecise — `0002` does not match committed `78e1484` (it still carries the uncommitted M2-D16 trigger-split edits), and `0003` is itself untracked, not part of that commit at all. Corrected wording in the milestone status line, M2-D21, and the M2-FQA-RR2-01 Risk Log row to state precisely what happened: `0002` was restored to its pre-this-packet worktree shape, not to `78e1484`. Also corrected `docs/runbooks/oauth-setup.md`: "version 3 or higher" → exact-equality wording matching `health.ts`'s actual comparison, and the migration-trigger instruction now covers any version other than 3 (previously only mentioned "if it reports 1", which did not cover the actual known preview state of 2). No application or SQL code changed | Complete; awaiting Codex re-review | Codex |

## Decision Log

| ID | Date | Decision | Owner | Rationale/impact |
|---|---|---|---|---|
| M2-D17 | 2026-07-26 | **Resolved: M2-AR-01's move rule is narrowed further, not simply kept as an outright refusal.** M2.5's handoff left one owner decision open: whether refusing every ownership-changing move (as M2-AR-01 shipped it) was correct, or whether Brian intended a narrower carve-out. Brian's answer, given directly: (1) the List owner may move their own task out of a List they own into a List owned by someone else, but only after explicitly confirming a warning that the task's private notes and history become visible only to the new owner — `TaskService.move` now takes a `confirmed` flag and throws `409 CONFIRMATION_REQUIRED` until it is `true`; (2) a non-owner (Editor or Admin without owning the source) moving a task into a List they own remains a hard `403`, unchanged from M2-AR-01; (3) clarified further mid-session — a non-owner may **not** move a task between two Lists owned by two *different* other people either, even though the mover acquires nothing. That third case was not covered by M2-AR-01 at all (`moveAcquiresOwnership` only checks the *destination* against the actor) and was an open gap this decision closes at the same time. The new policy: `canMoveTask` allows a cross-owner move only when the actor is the source List's owner (`moveRelinquishesOwnership`); `moveTaskDecision` adds the confirmation gate on top for that one case | Brian | Closes the M2.5 handoff's open decision and a second, previously-unidentified authorization gap in the same rule. `src/server/policy/sheet-access.ts` (`moveChangesTaskOwner`, `moveRelinquishesOwnership`, `moveTaskDecision`), `src/server/services/task-service.ts` (`move` takes `confirmed`), `src/shared/contracts/requests.ts` (`MoveTaskRequest.confirmed`), `src/server/routes/tasks.ts`. 9 new/updated tests in `test/integration/admin-privilege-escalation.test.ts`, `test/integration/authorization-service.test.ts`, `test/unit/authorization-matrix.test.ts`. No UI confirmation dialog built — this is the server-side contract only; a client must send `confirmed: true` after showing the warning, which is M3/M4's surface. |
| M2-D1 | 2026-07-24 | **Closed as already-satisfied, not a live decision.** This row predated M0's acceptance. The permission and lifecycle decisions M2 depends on are recorded: M0-D3 (Viewer/Editor/Owner, new shares default Viewer), M0-D4 + M0-D16 (private tasks/notes exist; Admin cannot read them), M0-D5 + M0-D22 (30-day recycle bin, `recycled_at`, explicit `recycled` account state, `disabled` not overloaded), M0-D6 (owner-only history field values), M0-D12 (canonical `owner_user_id`, UUIDs, SQL repositories, KV sessions), M0-D20 (profile basics). Authoritative matrix: Launch Contract §2; lifecycles: §3 | Brian (recorded in M0) / Claude (identified the row as stale) | Unblocks M2.1–M2.4 without a new decision. No M0 decision was reinterpreted. |
| M2-D2 | 2026-07-24 | **No D1 `sessions` table.** Launch Contract AC-D1 lists "sessions" among the schema items, but M0-D12 and the M0 approved decision record state that "KV is limited to opaque sessions and short-lived authentication/flow state." An explicitly approved architecture decision outranks a word in an acceptance-criterion list, so sessions stay in KV and D1's contribution to immediate revocation is `users.auth_version`. A test asserts no `sessions` table is created | Claude (precedence reading) | Keeps the approved isolation of session storage. **Flagged for Codex:** AC-D1's wording should be corrected so the next reader does not re-open this. |
| M2-QA-04-DECISION | 2026-07-25 | **Resolved: fail-open accepted as sufficient for launch.** `checkRateLimit`'s fail-open fix (catch a `kv.put` failure and allow the request rather than 500) correctly removed an availability bug, but Codex's re-review established that the same fail-open path means the nominal 20-requests/60-seconds bound on `GET /api/v1/auth/start` is **not enforced under concurrent traffic**: simultaneous requests from one source can race the same-key KV read/write and be waved through by the fail-open branch rather than counted, so a determined concurrent burst can still drive close to unbounded KV writes and provider-URL construction — narrower than a naive read of "20/60s" implies. Options were: (A) accept this as the residual risk and keep the simple fail-open counter, since the approved architecture calls for "rate-limit OAuth initiation" as an application control layered under Cloudflare's own edge protections, not a guaranteed precise cap, and a stricter design (time-bucketed keys, or a stronger primitive) is more engineering for a narrow gap; (B) redesign the limiter for real concurrent-safety. **Brian approved Option A.** The redirect-path length bound (512 chars) is unaffected and remains a hard, always-enforced limit regardless of this decision | Brian | Closes M2-QA-04 as an accepted residual risk rather than a hard guarantee. The Risk Log wording is corrected from "fully resolved" (inaccurate — Claude's own error, caught by Codex) to "resolved by accepted residual risk." No further code change follows; revisit only if real abuse against `/auth/start` is observed. |
| M2-QA-02-DECISION | 2026-07-25 | **Resolved: Option A approved.** Codex found that OAuth-state one-time consumption and session logout/revocation immediacy are implemented as KV get-then-delete/put-then-delete and described as atomic/immediate, but Cloudflare's own KV documentation states reads are eventually consistent and cross-location propagation can take up to 60 seconds, so neither guarantee is actually established under real traffic. The two structurally obvious fixes each cross an approved boundary — (a) a Durable Object, the natural strongly-consistent coordinator, is explicitly listed as not-yet-approved infrastructure in the technical architecture; (b) moving OAuth state or sessions into D1 for strong consistency would reopen M0-D12/M2-D2 directly. **Brian approved Option A:** accept the residual KV race as a documented, bounded risk. No architecture change; sessions and OAuth state stay in KV as already decided (M0-D12/M2-D2 stand, not reopened). The code's overstated "atomic"/"immediate" comments were already corrected to describe KV's actual consistency window rather than overclaiming (done in the 2026-07-25 correction pass, ahead of this approval). Practical exposure accepted as narrow: a determined attacker distributed across Cloudflare colocations within the same ~60s window, racing the exact request that consumes a specific one-time state or revokes a specific session | Brian | Closes M2-QA-02 without new infrastructure or reopening prior architecture decisions. Revisit only if a real incident occurs or a later feature (e.g. real-time updates) independently justifies approving Durable Objects — not a scheduled follow-up. |
| M2-D3 | 2026-07-24 | **No `dashboards` / `dashboard_sheets` tables in V2.** At launch there is one dashboard per user and the visible-List selection and ordering are device-local preferences (M0-D9; product plan "Device-local settings (V2) … visible Lists stored locally"). AC-D1 does not list them and M2's in-scope list does not include them. A test asserts they are absent, alongside the V2.1 seams (`invites`, `public_profiles`, `device_profiles`) | Claude (scope reading) | Avoids building an access-control-adjacent table V2 has no use for. If M3 finds it needs server-side dashboard configuration, that is a scope question for Brian, not a silent addition. |
| M2-D4 | 2026-07-24 | **Added four columns the approved architecture's table sketches omit**, each required by approved *behaviour*: `users.auth_version` (M2 in-scope "user/auth-version revocation"; architecture's session section calls for it in prose), `users.locale` (M0-D20 browser-derived locale), `tasks.is_private` (E3.5), `tasks.notes_private` (E5's note: notes are visible to editors/viewers "unless note is marked private", and Launch Contract §2 gives private-note reads to the owner only). Column naming and shape are routine implementation choices | Claude (within authority) | The behaviour was approved; only the storage spelling was open. **Flagged for Codex:** the technical architecture's `users`/`tasks` table sketches are now behind the schema and should be reconciled. |
| M2-D5 | 2026-07-24 | **Integration tests run in workerd against a real D1** via `@cloudflare/vitest-pool-workers` 0.18.8 (peer-compatible with the installed Vitest 4.1.10), added as the packet's only new dependency. Vitest now has two projects: `node` (existing contract tests, behaviour unchanged) and `integration`. Chosen over a `node:sqlite` shim because M2.1's acceptance criterion is explicitly "test actual SQL against a migrated database", and this applies the real migration files rather than a hand-built schema | Claude (within authority) | Makes the FK, CHECK, partial-index, and trigger evidence real. Cost: one devDependency and a workerd binary in CI. |
| M2-D14 | 2026-07-25 | **Origin enforcement is registered on `/api/v1/*`, which changes one M1 behaviour.** An unsafe request to an *unknown* `/api/v1` path now answers `403` (origin) instead of M1's catch-all `404`, because the origin check runs first. Kept deliberately: a route that does not exist must not be a CSRF-exempt hole, and confirming path existence to an unverified cross-origin caller is a small disclosure. Safe methods are unaffected — `GET` on an unknown path still returns the M1 `404` envelope, asserted by a test | Claude (within authority) | Documented in `src/server/index.ts` and pinned by two tests so the change is deliberate rather than incidental. **Flagged for Codex** as the one intentional M1 behaviour change in this packet. |
| M2-D13 | 2026-07-25 | **Session resolution is provider-free.** Validating a session reads KV and D1 only, so it is built without the OAuth provider. Found by a failing test: the first implementation built the full sign-in stack for every authenticated request, which made an environment lacking Google secrets answer `503 AUTH_NOT_CONFIGURED` to an ordinary invalid session — reporting a routine rejection as an outage, and coupling every authenticated request to config it does not need. Logout is provider-free for the same reason: signing out must always work | Claude (within authority) | A regression test asserts `401` for an unresolvable session while the env fixture has no OAuth config, so the coupling cannot silently return. |
| M2-D12 | 2026-07-25 | **Sessions carry a 90-day absolute ceiling alongside the approved 30-day sliding window.** M0 §8 fixes the sliding window; it is silent on an absolute cap. Without one, a session kept warm by use never expires, so a stolen token lives indefinitely. 90 days is three full sliding windows — long enough never to interrupt an ordinary active user, short enough that a forgotten session dies | Claude (within authority) | Adds a bound the approved decision did not address rather than reinterpreting it. **Flagged for Codex:** if Brian wants strictly "30-day sliding, no ceiling", this is the line to change. |
| M2-D11 | 2026-07-25 | **Session tokens and OAuth state are stored hashed (SHA-256) in KV, and the Google ID token is verified via the tokeninfo endpoint rather than local JWKS.** Hashing means a KV dump yields no usable credential. The tokeninfo choice is the weaker of the two verification options and is deliberate: local JWKS verification needs key fetching, caching, rotation, and clock-skew policy, all better built against a real OAuth client than guessed. The audience check against the configured client id is present either way | Claude (within authority) | Recorded as follow-up rather than left implicit: replacing tokeninfo with local JWKS verification is worth doing when M2-R4 is resolved. |
| M2-D10 | 2026-07-25 | **A denial for a private task is `404`, not `403`.** Returning `403` would confirm the task exists, defeating the feature: a caller could enumerate private tasks by status code. The same rule applies to a List the actor cannot reach. `403` is reserved for cases where the actor is permitted to know the object exists but lacks the right | Claude (within authority) | Tested explicitly, including that a genuinely absent object and a hidden one are indistinguishable. |
| M2-D9 | 2026-07-25 | **Task history writes `changes_json = '{}'` for now.** The approved full-fidelity history (actor, time, action, changed fields, complete before/after) is M4's deliverable (AC-M4). Writing a half-specified shape now would either box M4 in or produce records that look complete but are not. The event type, actor, and time — everything the metadata projection exposes — are recorded correctly | Claude (within authority) | **Flagged for Codex/M4:** history *entries* exist and are correctly scoped List-owner-only, but their before/after payload is deliberately empty until M4. |
| M2-D8 | 2026-07-25 | **An admin cannot disable or recycle their own account** (`409 SELF_TARGET_REFUSED`). Not a privacy rule but an availability one: the action is irreversible by the actor, and with a small trusted admin set it can leave the installation with no usable administrator. No approved decision covers this case | Claude (within authority) | Narrow, reversible choice; if Brian wants self-service admin lockout it is a one-line change. |
| M2-D7 | 2026-07-25 | **Administrative authority and content visibility are separate modules, and `adminMayReadProtectedContent()` returns the literal type `false`.** M0-D16 makes "higher role sees more" wrong for V2, so the rule cannot be expressed as a role comparison. The literal return type means a caller cannot even type-check a branch where an admin reads protected content. The administrative recovery service is a *separate class* with no access to a method returning a full `TaskRecord`, so the guarantee is structural rather than disciplinary | Claude (within authority) | Three independent barriers (SQL projection, policy, DTO); any one failing still leaves two. Reversing it is a product decision for Brian, not an implementation change. |
| M2-D15 | 2026-07-25 | **The existing `dash2-preview` environment is the staging target for Google sign-in. No separate staging environment will be created for M2.** Brian, asked directly whether to build a staging environment or reuse preview, chose preview. Consistent with M1-D2 (isolation by dedicated resource naming in the same Cloudflare account, not a separate account) and with what was already configured on 2026-07-25: the OAuth client, its redirect URI, and the Worker secrets all target `dash2-preview`. `docs/runbooks/environments.md` should continue to record staging as "does not exist" — that is now a deliberate choice rather than an open question | Brian | Closes the environment half of M2-R4. The remaining half was already satisfied: the Google OAuth client exists. M2's "isolated staging OAuth callback, D1, and KV" prerequisite is met by preview acting in that role. |
| M2-D16 | 2026-07-25 | **A migration file that defines a trigger contains nothing else except its single `schema_version` insert.** Wrangler's remote path splits migration files on `;` and cuts trigger bodies in half; local tooling does not reproduce it. Enforced by `test/unit/migration-remote-safety.test.ts` and documented in `docs/runbooks/preview-deployment.md`. Forced by the tooling, not a product choice | Claude (within authority) | Prevents a repeat of M2-R9. The rule is checked in CI rather than left to reviewer memory. |
| M2-D6 | 2026-07-24 | **Timestamps in the domain schema are epoch milliseconds**, matching `Date.now()` and the architecture's "epoch milliseconds consistently for instants" API rule. `due_date` stays date-only TEXT `YYYY-MM-DD`. Note the pre-existing `schema_version.applied_at` from M1 uses `unixepoch()` seconds; it is migration bookkeeping, is never exposed, and was left alone rather than rewritten | Claude (within authority) | One convention for all domain instants. The one-row inconsistency is recorded rather than hidden. |
| M2-D18 | 2026-07-26 | **Pre-QA decision sweep: Brian confirmed all outstanding M2 decisions in one pass.** (1) **M2-D12** (90-day absolute session ceiling) — confirmed as specified, no change. (2) **M2-D14** (origin check turns an unknown-path unsafe request from 404 to 403) — confirmed as specified, no change. (3) **M2.5 model deviation** (Fable 5 adversarial review; Sonnet 5 correction pass, both instead of the milestone's specified separate Opus 5 `xhigh`) — accepted as the record; no re-run required before QA. (4) **M2-R9 governance half** — Brian confirmed the 2026-07-25 remote migration against `dash2-preview` was in fact authorized in another session; the only gap was that no gate ID had been recorded in this repository at the time. (5) **M2-R7** — the live Google sign-in test is explicitly deferred to a later phase, not completed now; QA proceeds with the callback leg unverified. (6) **Commit timing** — holding off on committing the accumulated M2 work until M2-R7 is resolved, i.e. after Codex QA in this phase, not before | Brian | Closes every open M2 decision gate except M2-R7 itself (deferred, not resolved) and clears the way for Codex's final QA pass on the current uncommitted worktree. |
| M2-D19 | 2026-07-26 | **M2-FQA-01 through M2-FQA-06 corrected within Claude's implementation authority; no product/architecture decision was needed.** `decision-gate-audit` confirmed all six of Codex's full-QA findings are correctness/security bug fixes against already-approved invariants (M0-D16 private content, M0 §5 required audit evidence, M2's own ownership-confirmation decision), not new scope. Summary of fixes: **M2-FQA-01** — a migrated user's placeholder `provider_subject` is now rebound in place (`UserRepository.rebindProviderSubject`) on first real sign-in instead of a second `INSERT` that always collided with the globally-unique `email_normalized`. **M2-FQA-02** — `parseTaskFields`'s `isPrivate`/`notesPrivate` are now required fields with no default, so an omitted flag on a full replacement (PUT) fails validation instead of silently declassifying stored content. **M2-FQA-03** — new policy function `canWriteTaskAsPrivate` authorizes the *requested* `isPrivate` value on both create and update, closing the gap where a non-owner could produce or transition a task into owner-only content. **M2-FQA-04** — every repository with a mutation that requires history/audit evidence now exposes a `prepareX()` unexecuted-statement variant; `ServiceDeps` gained a shared `db: D1Database` handle so services batch the mutation and its required evidence rows in one `D1Database.batch()` call across repository boundaries, proven by two failure-injection tests that a genuinely rejected batch leaves no partial state. **M2-FQA-05** — `AdminRecoveryService.restoreTask` now appends the same allowlisted `restored` history event the owner's own restore path writes, batched atomically with the restore and its audit row. **M2-FQA-06** — `TaskService.move` now returns the *destination* List's access context alongside the moved task, so the route builds its response from that instead of re-authorizing the task as the (now former) owner, which incorrectly reported a successful confirmed relinquish as a 404 | Claude (within authority, per `decision-gate-audit`) | 18 new/updated regression tests across `test/unit/contracts.test.ts`, `test/integration/{auth-lifecycle,authorization-service,admin-recovery-boundary,admin-privilege-escalation}.test.ts`. Full suite: 535 tests passing (was 517). Not yet independently re-verified by Codex. |
| M2-D20 | 2026-07-26 | **Codex's re-review found three of the six M2-D19 fixes incomplete (M2-FQA-RR-01/02/03); all three corrected, within Claude's authority.** **M2-FQA-RR-01** — the M2-FQA-01 fix rebound *any* mismatched subject unconditionally, with no concept of "already permanently bound," so a reassigned email or changed OAuth subject could silently move an account between real people. Fixed with a schema addition, `user_identities.subject_pending`: a placeholder identity is `subject_pending = true`; the first real bind flips it permanently to `false` in the same guarded `UPDATE`, and no later differing subject can ever match that row again. **This schema addition was initially applied incorrectly — see M2-D21 for the correction.** **M2-FQA-RR-02** — the M2-FQA-03 fix authorized only the requested `isPrivate` state; `notesPrivate` was independently unauthorized, so a non-owner could still write `isPrivate: false, notesPrivate: true`, or edit/clear/un-private an already-private note. Fixed with a new `canWriteTaskNotesAsPrivate` policy function checking both the note's current and requested private state, applied on create and update. **M2-FQA-RR-03** — the M2-FQA-06 fix stopped the false 404, but the route still returned a full task DTO (name, status, priority, dates, privacy flags) to an actor `canReadTask` had just denied; only `notes` was conditionally redacted. Fixed with a new opaque `TaskMoveResultDto` (`{moved, taskId, destinationSheetId}`, no task field) returned when `canReadTask` denies the actor post-move | Claude (within authority) | 16 new/updated regression tests, including 3 covering concurrent-bind safety for M2-FQA-RR-01 (`Promise.allSettled` against real D1, exactly one attempt wins). Full suite: 551 tests passing (was 535) at the time of this entry; see M2-D21 for the corrected count. Not yet independently re-verified by Codex. |
| M2-D21 | 2026-07-27 | **Corrects a factual error in M2-D20: `subject_pending` was added by editing `migrations/0002_domain_schema.sql` in place, on the mistaken claim that `0002` was "still uncommitted/unshipped."** That claim was wrong and contradicted this same document's own M2-R3 and M2-R9 records: `0002` was applied to the real `dash2-preview` D1 on 2026-07-25 (M2-R9) and independently reconfirmed live during the M2-R3 restore rehearsal on 2026-07-26 — both recorded in this document before the false claim was written. D1 tracks applied migrations by filename, not content, so editing an already-recorded file does not schedule it to re-run: `dash2-preview` would never have received the column, and the corrected `UserRepository` code reads/writes `subject_pending` unconditionally, so a deploy there would fail on a missing-column SQL error on first identity access. Found by Codex's second re-review, 2026-07-27 (`M2-FQA-RR2-01`, P1). **Corrected 2026-07-27:** the in-place edit to `0002` was reverted to the shape it held in this worktree immediately before this correction touched it — the already-applied, post-trigger-split content (that content differs from committed `78e1484`; the original wording here claimed otherwise and was itself imprecise, corrected by M2-D22). The column now ships as a new append-only migration, `migrations/0004_identity_subject_pending.sql`, following the same one-file-per-change discipline M2-D16 established. `EXPECTED_SCHEMA_VERSION` moved 2 → 3. Verified against a fresh empty local D1 (all 4 migrations apply cleanly, idempotent re-apply reports nothing pending) and against a simulated upgrade path (migrations `0001`–`0003` applied and tracked in `d1_migrations` exactly as the real `dash2-preview` already has them recorded; a subsequent `migrations apply` run correctly identified only `0004` as pending and applied just that one file) | Claude (within authority) | Full suite: **552 tests** passing (was 551). `docs/runbooks/{oauth-setup,preview-deployment}.md` corrected from "schema version 2" to "schema version 3". Independently re-verified by Codex; see M2-D22 for one follow-on wording correction. |
| M2-D22 | 2026-07-27 | **Corrects a wording imprecision in M2-D21/M2-E9/the M2-FQA-RR2-01 Risk Log row, found by Codex's third re-review the same day (`M2-FQA-RR3-01`, P2).** Those records said `0002` was restored to "its exact prior committed shape," implying it now matches committed `78e1484`. It does not: `0002` still carries the uncommitted M2-D16 trigger-split edits from an earlier session, and `0003` is itself an untracked file that does not exist at `78e1484` at all. What is actually true, and all M2-D21's correction ever claimed to do: `0002` was restored to the shape it held in *this worktree* immediately before the M2-FQA-RR-01 packet touched it — the already-applied, post-trigger-split content, with only the `subject_pending` edit removed. Separately, `docs/runbooks/oauth-setup.md` said schema version "3 or higher" though `/api/v1/health` requires exact equality, and instructed migrating only "if it reports 1," which omitted the actual known preview state of 2 | Claude (within authority) | Documentation-only; no application or SQL code changed. Corrected the milestone status line, M2-D21, and the M2-FQA-RR2-01 Risk Log row; corrected the OAuth runbook's version wording and migration-trigger condition. Pending independent re-review. |
| M2-D23 | 2026-07-27 | **Milestone accepted.** After Codex's fifth review round (the M2-FQA-RR3-01 narrow re-review) recommended acceptance with 0 open P0/P1/P2, Brian accepted M2 explicitly carrying forward, not closing, one deferred item: the live Google OAuth callback/token-exchange verification (M2-R7) and applying migration `0004_identity_subject_pending.sql` to the real `dash2-preview` database, both deferred to a later work wave. Brian's instruction: "Accept without closing that step since I'm holding off until a later wave." This is a knowing risk acceptance of a specific, narrow, already-tested-except-for-live-provider gap — not an unnoticed omission. `dash2-preview` remains at schema version 2; the Worker there must not be redeployed with the corrected identity-binding code until `0004` is applied, and M2-R7 cannot be closed until it is, since the live callback needs a real account bound through the corrected path | Brian | Closes the milestone's exit gate. Does not authorize commit, push, deploy, or the deferred migration/OAuth actions — each remains separately gated per CLAUDE.md. |

## Risk Log

| ID | Severity | Risk | Mitigation/trigger | Owner | Status |
|---|---|---|---|---|---|
| M2-R1 | P0 | Authorization or private-field exposure | Central policy + exhaustive denial tests | Claude | **Mitigated at the implementation level 2026-07-25, pending independent review.** A central policy layer now exists and every service consults it before mutating; no route makes an authorization decision. 112 matrix assertions + 49 service denial tests + 21 unauthenticated route assertions. **This is not a claim that no bypass exists** — that is precisely what M2.5's adversarial review and Codex QA are for, and neither has run. Treat as "mitigated by construction and by the tests written, independently unverified". |
| M2-R2 | P1 | Administrative DTO exposes protected content | Allowlisted DTOs and direct denial tests | Claude | **Mitigated 2026-07-25, pending independent review.** All three layers are now in place: SQL projections that never select protected columns (M2.1), a policy layer that denies Admin protected reads with a literal-`false` return type, and DTOs built from named allowlists in a structurally distinct type. 15 boundary tests with exact-key assertions, marker scans, and positive controls. Any one layer failing still leaves two. |
| M2-R3 | P1 | Schema change cannot be safely restored | Staging restore rehearsal | Claude | **Resolved 2026-07-26.** Brian approved via `production-mutation-gate` (gate `PM-fe793662-e465-40f1-906d-d55c5e7cbc6a`). Rehearsal performed: `wrangler d1 export` against the real remote `dash2-preview` D1 (read-only, 165-line export: 10 tables, 3 triggers, 6 bookkeeping inserts, 0 content rows, matching the live state exactly), then the export was re-applied to a fresh, isolated **local** D1 (`.wrangler/state/m2r3-restore-test`, deleted after) to prove the restore path without a second remote write. Every statement applied without error; the restored database's object list (10 tables, 3 triggers, all `idx_*` indexes) and `schema_version = 2` matched exactly, with 0 rows in `users`/`sheets`/`tasks` as expected. Remote `dash2-preview` reconfirmed unchanged afterward (`changed_db: false` on every read, identical `size_after`). The export file was deleted from scratchpad after the rehearsal; it held no user content (the database has none). Teardown itself remains untested by design, consistent with the same scoping M1's rollback rehearsal used — it is a separate destructive action requiring its own gate if ever needed. |
| M2-R4 | P1 | **M2 prerequisite unmet: no isolated OAuth callback and no staging environment exist.** M2's Prerequisites require an "isolated staging OAuth callback, D1, and KV". Only the `dash2-preview` D1/KV exist; `docs/runbooks/environments.md` records staging as not yet created or decided, and no Google OAuth client has been created. M2.3 cannot be implemented or tested end to end without them | Brian creates or authorizes creation of a Google OAuth client and its redirect URI for an isolated Dash2 host, and decides whether M2.3 targets the existing preview environment or a new staging one | Brian | **Resolved 2026-07-25.** Both halves are now satisfied, and this entry's wording had gone stale on the first half before it was noticed. (a) **The OAuth client already existed:** Brian created it and added `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` as Worker secrets on 2026-07-25 with a matching redirect URI, recorded in `docs/runbooks/oauth-setup.md` and `environments.md` — while this row still claimed none existed. (b) **The environment question is decided:** preview is the staging target (M2-D15). Verified live and read-only 2026-07-25: `GET /api/v1/auth/start` on the deployed preview returns `302` to Google's real authorization endpoint with the configured client id, a redirect URI matching the registered one, `response_type=code`, `scope=openid email profile`, PKCE `code_challenge_method=S256`, and a 256-bit state. |
| M2-R6 | P3 | **Evidence-accuracy defect in the M2.1 handoff, found by Codex's review 2026-07-24 and confirmed.** The handoff reported "21 new / 9 modified" files against an actual diff of 22 new / 11 modified. Two distinct errors: the new count was a hand-count slip against a list that was itself correct and complete, and the modified count was stale — taken from a `git status` snapshot predating the edits to this document and `docs/milestones/README.md`. Same category as M1-R13, lower impact: M1-R13 misstated whether a real deploy had occurred and could have driven a wrong acceptance decision, whereas this misstates a file tally any reviewer confirms in one command, with the correct lists printed alongside it | **Resolved 2026-07-24.** Handoff counts corrected and now derived directly from `git ls-files --others --exclude-standard` and `git diff --name-only`; every other numeric claim in the handoff (test counts 23/112/135, applied objects 10/3/14, migration commands 3+27, lockfile +37 lines / 3 packages, bundle size) was re-checked against observed output and confirmed accurate. The handoff's "0 defects" wording was also narrowed to "0 found by Claude, independently unverified". Process correction: derive file counts as the last step before writing the handoff, after all edits | Claude | Resolved 2026-07-24; pending Codex re-review |
| M2-R9 | P2 | **A remote migration against the real preview database failed, was fixed, and none of it was recorded.** On 2026-07-25 `0002_domain_schema.sql` was applied to the `dash2-preview` D1 with `--remote` and failed — Wrangler's remote path splits on `;` and cut the ownership triggers in half (`incomplete input: SQLITE_ERROR [code: 7500]`). The triggers were split into `0003_ownership_triggers.sql` and the migration was re-run successfully. **Two separate problems.** (1) *Evidence:* the only trace of any of this was prose inside two untracked files; the Risk Log, Evidence Index, and Decision Log said nothing, and the M2.2–M2.4 handoff section written by that same session states "no migration, no remote database … was touched" — a checked-in record actively contradicting what happened. (2) *Governance:* a real mutation against a real Cloudflare database occurred with no `production-mutation-gate` ID recorded, so from the repository alone it cannot be established whether Brian authorized it. Severity is P2 rather than P1 because the outcome is verified correct and nothing was at risk: the database held zero rows, and the migration is additive-only | **Technical half resolved 2026-07-25.** Split verified: `0001`+`0002`+`0003` apply from a deleted `.wrangler/state` twice (3 + 23 + 5 commands) with an idempotent re-apply in between, producing 10 tables / 3 triggers / 14 indexes / `schema_version = 2`. Remote state independently re-verified read-only the same day: "No migrations to apply!" and the identical object counts with 0 users / 0 sheets / 0 tasks. Regression guard added (`test/unit/migration-remote-safety.test.ts`) and the rule documented (M2-D16, `docs/runbooks/preview-deployment.md`). Record corrected here and in the handoff. **Governance half is Brian's:** confirm whether that remote apply was authorized at the time | Claude (technical) / **Brian** (authorization question) | **Technical: Resolved 2026-07-25. Governance: open, awaiting Brian.** |
| M2-R10 | P3 | **The deployed preview Worker reported `version: 0.1.0-m1` while serving M2 code.** `APP_VERSION` in `wrangler.jsonc` was never bumped when M2 shipped, so `/api/v1/health` — the endpoint whose stated purpose is reporting what is deployed — understated it by a whole milestone. Found by reading the live response during this pass | **Resolved 2026-07-25.** `APP_VERSION` set to `0.2.0-m2` at both the top level and under `env.preview`. Takes effect on the next deploy; no deploy was performed | Claude | Resolved 2026-07-25; the live value stays `0.1.0-m1` until someone deploys |
| M2-R7 | P2 | **The Google provider adapter has never contacted Google.** `src/server/auth/google-provider.ts` follows Google's published OpenID Connect contract and its error paths are tested with synthetic responses, but no live exchange has occurred because M2-R4's prerequisite is unmet. Endpoint URLs, parameter names, claim names, and the tokeninfo response shape are therefore *unverified assumptions*. A mistake in any of them would surface only on the first real sign-in | The `IdentityProviderClient` seam confines the risk to one file; everything else in the auth path is tested against real KV/D1 with a fake provider. **Partially reduced 2026-07-25** now that M2-R4 is resolved: a live read-only `GET /api/v1/auth/start` against the deployed preview returns `302` to Google's real authorization endpoint, so the authorization-request half — endpoint URL, client id, registered redirect URI, scope, PKCE S256 challenge, and state — is now verified against real configuration rather than assumed. **Still unverified: the callback half** — the token exchange, the tokeninfo call, and the claim names read from its response. Those cannot be reached without a human completing a real Google sign-in in a browser, which Claude cannot do. Resolve by having Brian sign in once against preview, then convert tokeninfo to local JWKS verification (M2-D11) | Claude (authorization half verified) / **Brian** (one real sign-in) | **Open, accepted as deferred at milestone acceptance (M2-D23, 2026-07-27).** Raised 2026-07-25, narrowed the same day from "never contacted Google" to "the callback legs are unexercised". Brian confirmed the live sign-in test would not gate M2's QA rounds (2026-07-26), and on acceptance explicitly chose to carry it forward to a later work wave rather than complete it now. Also blocks on migration `0004` being applied to `dash2-preview` first (M2-FQA-RR2-01) — the corrected identity-binding code needs `subject_pending` to exist before a real callback can succeed. Not a defect the milestone failed to catch: a known, narrow, explicitly accepted residual risk. |
| M2-R8 | P3 | **`wrangler dev --local` on this Windows machine crashes workerd when a request body is never read**, which happens on every origin/auth rejection. Manifests as the *next* request failing with "Network connection lost" / HTTP 500 | **Confirmed not a Dash2 defect.** Isolated by `git stash` to pristine M1 (`5a00b54`), where `wrangler dev` crashed with `*** std::terminate() called with no exception` before any M2.2–M2.4 code was present; a bare non-Dash2 Worker with the same request shape survives, so it is specific to this configuration's workerd binary on Windows, not to the rejection logic. All 446 automated tests — which run in workerd via the vitest pool, not the dev server — pass. Impact is on local manual probing only | Claude | **Open — local tooling only.** Raised 2026-07-25. No production impact known; worth re-checking on `ubuntu-latest` CI and after a wrangler upgrade. |
| M2-FQA-01 | P1 | **A migrated user's first real Google sign-in could never succeed.** `resolveEligibleUser`'s email-fallback path called `createIdentity` (a plain `INSERT`) to bind the verified subject, but `email_normalized` is globally unique and the fallback match is keyed on that same email, so the insert always collided with the placeholder row it was supposed to replace. Found by Codex's full-QA pass, 2026-07-26 | **Resolved 2026-07-26, then found incomplete by Codex re-review the same day (M2-FQA-RR-01), re-resolved 2026-07-26.** First fix: `UserRepository.rebindProviderSubject` rebinds via `UPDATE` instead of a colliding `INSERT`. Re-review found the rebind was unconditional — any differing subject could rebind the row indefinitely, so a reassigned email or changed OAuth subject could silently move an account to a different real person. Second fix: schema gained `user_identities.subject_pending`; the rebind now requires `subject_pending = 1` and permanently flips it to `0` in the same statement, so no later differing subject can ever match again. 7 integration tests total against real D1, including 3 covering the permanent-binding boundary and one proving exactly one of two concurrent bind attempts wins | Claude | Resolved 2026-07-26 (twice); pending independent re-review. M2-R7 (live callback) remains the only way to prove this against real Google, and is separately deferred. |
| M2-FQA-02 | P1 | **A full task replacement (PUT) that omitted `isPrivate` or `notesPrivate` silently declassified existing private content.** `parseTaskFields` defaulted both flags to `false` when absent; the comment's claim that "update callers send the complete intended state" was asserted, not enforced. Found by Codex's full-QA pass, 2026-07-26 | **Resolved 2026-07-26.** Both fields are now required, no default; an omission fails `400 VALIDATION_FAILED` naming the field. 4 new unit tests in `test/unit/contracts.test.ts` | Claude | Resolved 2026-07-26; pending independent review |
| M2-FQA-03 | P1 | **Task creation and update authorized only the List-level write right, never the requested `isPrivate` value**, so a non-owning Editor or Admin could create owner-only content directly, or flip an ordinary task to private — producing protected content a non-owner could never have written into an *existing* private task. The update route's post-write reauthorization then reported the resulting (now-inaccessible-to-them) task as a 404 even on the create path's silent success. Found by Codex's full-QA pass, 2026-07-26 | **Resolved 2026-07-26, then found incomplete by Codex re-review the same day (M2-FQA-RR-02), re-resolved 2026-07-26.** First fix: `canWriteTaskAsPrivate(actor, sheet, targetIsPrivate)` authorizes the requested `isPrivate` value on create/update. Re-review found `notesPrivate` was still unguarded — a non-owner could write `isPrivate: false, notesPrivate: true`, or edit/clear/un-private an already-private note, since the task-level check alone never inspected the independent note-privacy axis. Second fix: new `canWriteTaskNotesAsPrivate(actor, sheet, currentNotesPrivate, targetNotesPrivate)` checks both the note's stored and requested state, applied alongside the task-level check on both create and update. 11 integration tests total against real D1, covering create/transition/edit/clear/un-private on both axes with owner-positive controls | Claude | Resolved 2026-07-26 (twice); pending independent re-review |
| M2-FQA-04 | P1 | **Every mutation requiring history or audit evidence wrote its state change and that evidence as two separate D1 statements**, not one atomic batch — task update+history, task restore+history+admin-audit, membership grant/revoke+audit, ownership transfer+audit, user disable/recycle/role-change+audit, opaque admin restore/purge+audit. A failure on the second statement left the mutation committed with no required evidence. Found by Codex's full-QA pass, 2026-07-26 | **Resolved 2026-07-26.** Every repository with an affected mutation now exposes a `prepareX()` unexecuted-statement variant (`TaskRepository`, `TaskEventRepository`, `AuditEventRepository`, `SheetRepository`, `MembershipRepository`, `UserRepository`); `ServiceDeps` gained a shared `db: D1Database` handle so services batch the mutation and its required evidence in one `D1Database.batch()` call, including across repository boundaries. 2 new failure-injection integration tests prove it: a genuinely rejected second statement (an invalid task-history foreign key; an over-length audit `metadata_json` past the schema's own `CHECK` bound) leaves the first statement's mutation unapplied | Claude | Resolved 2026-07-26; pending independent review |
| M2-FQA-05 | P2 | **Opaque Admin task restore did not append the `restored` task-history event** the owner's own restore path writes, leaving owner-visible history incomplete for a task recovered through the administrative surface even though the task itself was genuinely restored. Found by Codex's full-QA pass, 2026-07-26 | **Resolved 2026-07-26.** `AdminRecoveryService.restoreTask` now appends the same allowlisted `restored` event (no content, `changesJson: '{}'`), batched atomically with the restore and its audit row (folds into the M2-FQA-04 mechanism). 1 new integration test asserts the event exists and the opaque DTO's count reflects it | Claude | Resolved 2026-07-26; pending independent review |
| M2-FQA-06 | P2 | **A confirmed relinquishing move of a private task reported 404 after a successful mutation.** The move route re-authorized the moved task as the acting (now former) owner via a fresh `getById`, which correctly denies a private task to a non-owner — but that denial fired on the mover's *own* just-completed, successful, confirmed action. Found by Codex's full-QA pass, 2026-07-26 | **Resolved 2026-07-26, then found incomplete by Codex re-review the same day (M2-FQA-RR-03), re-resolved 2026-07-26.** First fix: `TaskService.move` returns `{ task, context }` using the destination context, avoiding the false 404. Re-review found the route still built a full `TaskDto` (name, status, priority, dates, privacy flags) from that result even when `canReadTask` denied the actor — only `notes` was conditionally redacted. Second fix: new opaque `TaskMoveResultDto` (`{moved, taskId, destinationSheetId}`, no task field) returned instead of the full DTO whenever `canReadTask` denies the actor post-move. 4 integration tests total: usable non-throwing result, `canReadTask` false confirmed, opaque-response marker scan (no task name/note text serialised), and a positive control proving the full DTO still returns when the actor retains read access | Claude | Resolved 2026-07-26 (twice); pending independent re-review |
| M2-FQA-RR-01 | P1 | **Any verified subject could replace a permanently bound subject by reusing its email.** The M2-FQA-01 fix rebound *any* identity row whose stored subject differed from the one presented, with no concept of "already permanently bound" — so a reassigned Workspace email, a changed OAuth-client subject, or any other same-email/different-subject condition could silently transfer an existing account to a different real person, and repeating the process could move it back. Found by Codex's re-review, 2026-07-26 | **Application logic resolved 2026-07-26; delivery mechanism found broken by a second re-review and corrected 2026-07-27 (M2-FQA-RR2-01).** Schema gained `user_identities.subject_pending`. A migrated identity is created with `subject_pending = true`; `rebindProviderSubject`'s `UPDATE` requires `subject_pending = 1` in its `WHERE` clause and sets it to `0` in the same statement, so the bind is a genuine one-way transition — a second differing subject can never match that row again. 3 integration tests against real D1: subject B refused after subject A is permanently bound, repeat login on A still works after B was refused, and exactly one of two concurrent first-bind attempts wins (`Promise.allSettled`). This logic was correct from the start; what was wrong was *how the column reached the database* — see M2-FQA-RR2-01 | Claude | Resolved 2026-07-27 (application logic 2026-07-26, delivery mechanism 2026-07-27); pending independent re-review |
| M2-FQA-RR2-01 | P1 | **`subject_pending` had no upgrade path for the already-migrated preview database.** The M2-FQA-RR-01 fix added the column by editing `migrations/0002_domain_schema.sql` in place, on the claim that `0002` was "still uncommitted/unshipped" — false, and contradicted this document's own M2-R9/M2-R3 records showing `0002` was applied to the real `dash2-preview` D1 on 2026-07-25 and reconfirmed live on 2026-07-26. D1 tracks applied migrations by filename; editing an already-recorded file is never re-run, so `dash2-preview` would never receive the column while the corrected `UserRepository` code reads/writes it unconditionally — a deploy there would fail with a missing-column SQL error on first identity access. Found by Codex's second re-review, 2026-07-27 | **Resolved 2026-07-27 (M2-D21), independently confirmed by a third Codex re-review the same day** (fresh apply, upgrade-from-`0001`–`0003` selecting only `0004`, existing-row backfill to `subject_pending = 0`, invalid-value CHECK rejection, and version advancement to 3 all independently reproduced). The in-place edit to `0002` was reverted to the shape it held in this worktree immediately before this correction — the already-applied, post-trigger-split content, not committed `78e1484` (that distinction was itself imprecise in the first write-up; see M2-FQA-RR3-01). `subject_pending` now ships as a new append-only migration, `migrations/0004_identity_subject_pending.sql` (`EXPECTED_SCHEMA_VERSION` 2 → 3), following the same one-file-per-change discipline M2-D16 established for the trigger split | Claude | Resolved 2026-07-27; independently verified by Codex. **Not yet applied to the real `dash2-preview` database** — that requires a separate `production-mutation-gate` action Brian has not yet authorized |
| M2-FQA-RR3-01 | P2 | **The corrected migration record still misstated Git history, and the OAuth runbook gave an incomplete upgrade instruction.** M2-D21/M2-E9/the M2-FQA-RR2-01 row all described `0002` as restored to "its exact prior committed shape," but `0002` does not match committed `78e1484` (it still carries the uncommitted M2-D16 trigger-split edits from an earlier session) and `0003` is itself untracked — neither exists in that form at `78e1484`. Separately, `docs/runbooks/oauth-setup.md` said schema version "3 or higher" though `health.ts` requires exact equality, and instructed migrating only "if it reports 1," which did not cover the actual known preview state of 2. Found by Codex's third re-review, 2026-07-27 | **Resolved 2026-07-27.** Corrected the imprecise history claim in the milestone status line, M2-D21, and the M2-FQA-RR2-01 row above to state precisely what happened: `0002` was restored to its pre-this-packet worktree shape, not to `78e1484`. Corrected the OAuth runbook to state exact-equality version 3 and to instruct migrating on any version other than 3 | Claude | Resolved 2026-07-27; pending independent re-review. Documentation-only — no application or SQL code changed |
| M2-FQA-RR-02 | P1 | **Non-owners could still create or transition into a private note.** `canWriteTaskAsPrivate` authorized only the requested `isPrivate` state; `notesPrivate` was persisted but never authorized as an independent protected-content axis, so a non-owner could write `isPrivate: false, notesPrivate: true` on create, or edit/clear/un-private an already-private note on update, none of which the M2-FQA-03 fix caught. Found by Codex's re-review, 2026-07-26 | **Resolved 2026-07-26.** New `canWriteTaskNotesAsPrivate(actor, sheet, currentNotesPrivate, targetNotesPrivate)` denies unless the actor owns the List whenever either the note's stored or requested state is private — symmetric with how `canWriteTask` already treats `isPrivate`. Consulted on both `create` (current always `false`) and `update` (current from the stored task). 6 new integration tests against real D1: denial for Editor/Admin creating with `notesPrivate: true`, denying a public-to-private note transition, denying an edit to an already-private note's text, denying un-privating an already-private note, and owner-positive controls for all of the above | Claude | Resolved 2026-07-26; pending independent re-review |
| M2-FQA-RR-03 | P2 | **A confirmed private-task relinquishment returned protected task fields to the former owner.** The M2-FQA-06 fix stopped the false 404, but the route still called `toTaskDto()` unconditionally, which returns the full task shape (name, status, priority, dates, privacy flags) and redacts only `notes` — for a private task the former owner fails `canReadTask` entirely, so returning any task field at all crosses the owner-only read boundary they just voluntarily relinquished. Found by Codex's re-review, 2026-07-26 | **Resolved 2026-07-26.** New `toTaskMoveResultDto(taskId, destinationSheetId)` builds an opaque `{moved: true, taskId, destinationSheetId}` result with no task field. The move route now checks `canReadTask` after the move and returns the opaque result when it is `false`, the full `TaskDto` otherwise. 4 new integration tests: `canReadTask` confirmed `false` for the relinquished private task, a route-equivalent marker-scan test proving neither the task name nor note text appear anywhere in the serialised opaque response, and a positive control proving the full DTO still returns when the actor retains read access via ordinary List membership at the destination | Claude | Resolved 2026-07-26; pending independent re-review |
| M2-R5 | P3 | Canonical planning documents are behind the implemented schema: the technical architecture's `users`/`tasks` table sketches omit `auth_version`, `locale`, `is_private`, and `notes_private` (M2-D4), and Launch Contract AC-D1 lists "sessions" as a D1 schema item although sessions are KV-only (M2-D2) | Reconcile both documents during Codex QA rather than leaving a future reader to rediscover the discrepancy. No code change implied | Codex | Open — documentation only, non-blocking |
| M2-R12 | P2 | **`grantMembership` did not check target-user eligibility, unlike the parallel and tested `transferOwnership` check.** Found by Claude's first-pass self-review 2026-07-25 (Sonnet 5, not the milestone's specified Opus 5 `xhigh` — a recorded deviation, not a finding). A disabled or recycled account could be granted a viewer/editor share; the grant was inert while the account stayed ineligible but would silently reactivate on restore, with no re-approval | **Resolved 2026-07-25.** `grantMembership` now refuses (`409 INELIGIBLE_MEMBER`) a target whose `state !== 'active'`, mirroring `transferOwnership`. Two regression tests added (`test/integration/authorization-service.test.ts`): disabled and recycled targets are both refused | Claude | Resolved 2026-07-25; pending independent review |
| M2-R13 | P3 | The admin task-restore route hardcoded `historyEventCount: 0` in its response instead of the real count the GET route on the same surface reports. Found by the same review pass | **Resolved 2026-07-25.** `AdminRecoveryService.restoreTask` now returns a `TaskRecoveryView` (task + real count via `taskEvents.countForTask`), matching `getTaskRecoveryState`'s shape; the route was simplified to pass it straight through | Claude | Resolved 2026-07-25; pending independent review |
| M2-R14 | P3 | `timingSafeEqual` in `src/server/auth/tokens.ts` was dead code — defined, exported, never called anywhere. No functional impact (KV lookups are by hashed key, not by raw comparison in application code), but unused security-primitive code invites confusion about what actually defends against timing attacks | **Resolved 2026-07-25.** Removed; no call site existed to replace | Claude | Resolved 2026-07-25 |
| M2-R11 | P2 | **The migration-remote-safety guard test did not actually work when it was first written.** It counted leftover statements after removing the trigger body and permitted one, so a trigger placed beside a single `CREATE TABLE` would still have shipped — the guard had only ever been exercised on known-good input, which cannot distinguish a working detector from one that always passes. Found in the 2026-07-25 handover-readiness correction pass, only in sign-off prose until this entry — never previously in this table (Codex M2-QA-05) | **Resolved 2026-07-25.** Fixed to check *what* the leftover statements are rather than counting them; proven by six tests against deliberately-bad SQL in `test/unit/migration-remote-safety.test.ts` | Claude | Resolved 2026-07-25; recorded here (was sign-off-only) per Codex M2-QA-05 |
| M2-QA-01 | P1 | **A recycled List, or a List owned by a recycled/disabled account, remained reachable and mutable through ordinary (non-lifecycle) authorization.** `SheetService.authorize` checked only actor ownership/membership, never `sheet.state` or the owner's account state, so existing Viewer/Editor/Owner/Admin members could keep reading and Editor+ could keep writing tasks in a List the contract says must "disappear for other members until restore" (owned-but-hidden, not merely read-only). Found by Codex's whole-milestone QA, 2026-07-25 | **Resolved 2026-07-25.** `SheetService.authorize` now denies (404) a recycled List and a List whose owner is not `active`, for every non-admin actor; an `options.allowRecycled` escape hatch lets `recycle`/`restore`/`purge` still reach a recycled List to act on it. `SheetRepository.listAccessibleActive`'s membership branch now joins the owner's account state. 9 new regression tests in `test/integration/authorization-service.test.ts` cover List-level recycle, owner-account recycle/disable, every role, Admin's continued reach, and restoration | Claude | Resolved 2026-07-25; pending Codex re-review |
| M2-QA-02 | P1 | **Workers KV cannot provide the one-time OAuth state or immediate session invalidation the code's comments claim.** `OAuthStateStore.consume` (get-then-delete) and `SessionStore.refresh`/logout (independent put/delete) are described as atomic/immediately-final, but Cloudflare's own KV documentation states reads are eventually consistent and cross-location propagation can take up to 60 seconds, with a 1-write-per-second-per-key limit and last-write-wins. Two concurrent OAuth callbacks could both read the same state before either delete propagates; a logout delete or a revocation-driven auth_version check could remain invisible from another location for up to 60 seconds. Found by Codex's whole-milestone QA, 2026-07-25 | **Resolved 2026-07-25 — Brian approved Option A (M2-QA-02-DECISION):** accepted as a documented, bounded residual risk rather than fixed with new infrastructure. Sessions/OAuth state stay in KV (M0-D12/M2-D2 unchanged); the code's overstated "atomic"/"immediate" comments in `oauth-state.ts`/`session.ts` were corrected to state KV's actual consistency window. Practical exposure: a determined attacker distributed across Cloudflare colocations within the same ~60s window, racing the exact request that consumes a specific one-time state or revokes a specific session. Revisit only on a real incident or if a later feature independently justifies Durable Objects | Claude (assessed) / **Brian (approved Option A)** | **Resolved 2026-07-25 — risk accepted, no code change required.** |
| M2-QA-03 | P1 | **A partial account-state update could resurrect a pre-recycle session after restore.** Disable/recycle/role-change each issued the state/role change and the `auth_version` bump as two separate D1 statements; if the bump failed after the state change landed, the account became ineligible (blocking the old session while ineligible) but its stored auth_version still matched, so a later restore would silently make the old session valid again — contradicting "restore cannot resurrect an old session." Found by Codex's whole-milestone QA, 2026-07-25 | **Resolved 2026-07-25.** `UserRepository.disable`, `.recycle`, and the new `.updateGlobalRoleAndRevoke` now perform the state/role change and the `auth_version` bump in one `D1Database.batch()` — both land or neither does. Regression coverage: `test/integration/user-repository.test.ts` (atomicity of the bump) and `test/integration/auth-lifecycle.test.ts` (end-to-end: pre-disable/recycle session stays dead through a subsequent restore) | Claude | Resolved 2026-07-25; pending Codex re-review |
| M2-QA-04 | P2 | **`GET /api/v1/auth/start` had no application rate limit and no bound on the redirect-path length**, contradicting the approved architecture's "rate-limit OAuth initiation" requirement. An anonymous caller could drive unbounded KV writes and provider-authorization-URL construction, and an arbitrarily long redirect path inflated every OAuth-state KV record. Found by Codex's whole-milestone QA, 2026-07-25 | **Resolved by accepted residual risk, 2026-07-25 (see M2-QA-04-DECISION).** The redirect-path bound (512 chars) is fully resolved and unaffected. The rate limiter (`src/server/auth/rate-limit.ts`, KV-backed, nominally 20 requests/60s per `CF-Connecting-IP`) had two successive defects: first, an uncaught `kv.put` failure under Cloudflare's one-write-per-second-per-key limit could 500 an ordinary rapid legitimate request — fixed by failing open on a write conflict. **Codex's second re-review then found the fail-open fix, while correctly removing the 500, means the nominal 20/60s bound is not actually enforced under concurrent traffic**: concurrent requests can read the same prior count and race the same-key write, so most of a simultaneous burst is waved through by the fail-open path rather than counted. Brian reviewed this and explicitly accepted the residual gap as sufficient for launch rather than requiring a redesigned limiter (M2-QA-04-DECISION) — this control remains a best-effort deterrent against a single slow-paced abuser, not a hard concurrent-burst cap | Claude (implementation) / **Brian (risk acceptance)** | **Resolved by accepted residual risk, 2026-07-25.** |
| M2-QA-05 | P2 | **The checked-in milestone record contradicted itself**: the top status and M2-E3 still said no OAuth client/staging existed after M2-D15/M2-R4 recorded the opposite; M2-R3 and the PM/QA sign-off still said no remote database was touched after M2-R9 recorded a real remote migration and `HEAD` became the committed M2 delivery; the sign-off called M2-R4 both resolved and blocking. Found by Codex's whole-milestone QA, 2026-07-25 | **Resolved 2026-07-25.** Top status, M2-E3, and M2-R3 rewritten to state the single current account: M2.1–M2.4 committed, OAuth client/preview-staging resolved (M2-D15/M2-R4), one remote migration applied and verified (M2-R9), restore rehearsal still open. M2-R11 (previously sign-off-prose-only) added to this table | Claude | Resolved 2026-07-25; independently confirmed by Codex re-review |
| M2-RR-01 | P1 | **Regression introduced by the M2-QA-01 fix: disabling (not recycling) a List owner made the List disappear for that owner's existing Editors/Viewers too.** `SheetService.authorize` and `SheetRepository.listAccessibleActive` checked `owner.state !== 'active'` / `= 'active'`, which excludes `disabled` as well as `recycled` — but `AccountService.disable`'s own contract is "the account keeps owning its Lists," and only *recycling* triggers the disappear-until-restore rule (M0 §Accounts, M0-D22). Found by Codex's `M2 Re-review`, 2026-07-25, of the M2-QA-01 fix | **Resolved 2026-07-25.** Both checks narrowed to `owner.state === 'recycled'` specifically. The regression test that had pinned the wrong behavior ("denies... once the owner account is disabled") was corrected to prove the opposite: existing Editor/Viewer access, task access, and the accessible-Lists listing all survive a disabled owner, while the recycled-owner denial and restore-reachability tests are unchanged | Claude | Resolved 2026-07-25; pending Codex re-review of this specific correction |
| M2-AR-01 | P1 | **Protected content was readable by acquiring ownership of it — the administrative privacy boundary could be walked around rather than through.** Content visibility derives from `ownerUserId === actor.userId`, and M2 defended that with three barriers on the *administrative* surface (allowlisted SQL projections, a literal-`false` policy function, structurally distinct recovery DTOs). None of them applies once the actor becomes the owner, and both sides of that comparison were writable. (1) `SheetService.transferOwnership` let an Admin name **themselves** the new owner of any List — `canTransferOwnership` → `canManageMembership` → `isAdmin`, with nothing checking the destination — after which every private task, private note, and history value in it was readable through the ordinary owner path. (2) `TaskService.move` enforced §2's "edit rights on both Lists" but never asked whether the move *acquired* ownership, so moving a task into a List you own made you its owner and handed over its private note and history values. **Path (2) is not admin-only: any Editor on a shared List can walk it**, which makes this an ordinary-collaborator disclosure rather than only an administrative defect. Found by M2.5's adversarial review, 2026-07-25, and reproduced against real migrated D1 — a temporary proof asserting the *bad* outcome passed pre-fix for the note via transfer, the note via move, owner-only history via transfer, and the Editor variant. **P1 not P0** only because nothing is exposed today: V1 is still production, the preview D1 holds 0 rows, and no real Dash2 user content exists. Shipped as written it would be P0. Neither the implementation passes, the Sonnet self-review, nor three Codex passes found it | **Resolved 2026-07-25.** Fixed at the policy layer so no service or route can miss it: new `canAssignOwnershipTo(actor, sheet, newOwnerUserId)` refuses naming yourself owner of a List you do not already own (`SheetService.transferOwnership` → 403); `canMoveTask` now refuses a move whose destination is owned by the actor when the source is not, via a new `moveAcquiresOwnership` predicate. Reassigning a List to a third party, moving within your own Lists, moving between other people's Lists, and giving one of your own tasks away are all unaffected. 14 integration tests against real D1 (`test/integration/admin-privilege-escalation.test.ts`) plus 11 policy assertions in `test/unit/authorization-matrix.test.ts`; 4 of the 14 failed pre-fix while the legitimate-path controls passed | Claude | **Resolved 2026-07-25; pending Codex re-review.** Carries one Brian-owned decision: the move rule narrows §2's literal "edit rights on both Lists" wording — see Unresolved decisions in the M2.5 handoff. |
| M2-AR-02 | P3 | **An administrative task move writes no audit row.** `TaskService.restore` and `.purge` write `task.restored.admin` / `task.purged.admin` when an admin acts on a List they do not own; `move`, `update`, `create`, and `recycle` do not. A `task_events` row records the actor and event type, so a trail exists, but the administrative nature of the override is not audited — against M2's "Admin overrides create required audit evidence" invariant. Found by M2.5, 2026-07-25 | **Recorded, not fixed.** `AUDIT_ACTIONS` has no task-move entry, and extending the approved audit vocabulary is M4's deliverable (AC-M4), not a review packet's call. Lower impact now that M2-AR-01 closes the ownership-acquiring move entirely — the remaining unaudited admin moves are between Lists owned by other people, which grant the admin nothing | Claude / M4 | Open — deferred to M4 |
| M2-AR-03 | P3 | **`GoogleIdentityProvider.verifyIdToken` checks `aud` but not `exp` or `iss` explicitly.** Google's tokeninfo endpoint rejects expired tokens with a non-200 and only issues Google tokens, so both are implicitly covered today; the audience check — the one that actually matters — is present. Found by M2.5, 2026-07-25 | **Recorded, not fixed.** Make both explicit when M2-D11's local-JWKS follow-up replaces tokeninfo, where they stop being implicit. Tied to M2-R7: this file remains unverified against live Google | Claude | Open — follow-up with M2-D11 |
| M2-RR-02 | P2 | **The M2-QA-02 code comments and one acceptance-criterion line still described the KV-consistency question as "decision pending" and made an unqualified "safe against replay" claim after Brian had already approved Option A.** A reviewer reading only the code or only that AC line would reach the opposite conclusion from the Decision Log and Risk Log. Found by Codex's `M2 Re-review`, 2026-07-25 | **Resolved 2026-07-25.** `oauth-state.ts` and `session.ts` comments now state Brian's approval of Option A rather than "pending." The AC line for OAuth-state one-time consumption is now marked `[~]` (partially satisfied) and qualified: safe against replay *within a single Cloudflare location* (what Miniflare's single-location simulation actually tests); the cross-location race is named as the accepted residual risk rather than left as an absolute claim | Claude | Resolved 2026-07-25; pending Codex re-review |

## PM/QA Sign-off

```text
Claude status: In Progress — M2.1, M2.2, and M2.4 Ready for PM/QA at the packet level; M2.3 Partial.
  Packets M2.2 (policy/services), M2.3 (OAuth/sessions), and M2.4 (contracts/DTOs) implemented and
  self-verified 2026-07-25, on top of M2.1 (2026-07-24). Milestone-level status stays In Progress for two
  reasons: M2.5 (adversarial review) was explicitly excluded from this run by Brian, and M2.3 cannot be
  fully verified until M2-R4 is resolved. Nothing is committed; nothing was deployed; no remote database
  was migrated; no Cloudflare or Google resource was created.
Claude handoff date: 2026-07-25
Codex review: Pending for M2.2/M2.3/M2.4 — no independent review of this packet has occurred. The earlier
  2026-07-24 Codex read covered the M2.1 handoff only, was explicitly not a code review, and its three
  flagged areas remain open claims. Nothing in this packet has been independently verified.
Open P0/P1: 0 defects **found by Claude** during M2.2–M2.4 implementation or self-verification — again a
  statement about what this pass found, not a claim that none exists. One real defect *was* found and fixed
  during the pass, by a test rather than by inspection: session resolution built the full OAuth stack and
  so answered 503 instead of 401 when the provider was unconfigured (M2-D13, regression test added). That
  is the second worked example in this milestone of something self-review missed until something external
  caught it. Open risks: M2-R1 (P0) now mitigated by construction but independently unverified — M2.5 exists
  precisely to test that claim; M2-R2 (P1) mitigated across all three layers, unverified; M2-R3 (P1) open,
  no remote/restore rehearsal; M2-R4 (P1, Brian-owned) still blocks M2.3's live verification; M2-R7 (P2,
  new) the Google adapter has never contacted Google; M2-R8 (P3, new) local wrangler dev instability,
  confirmed pre-existing and not a Dash2 defect.
Brian decision: Pending
Correction pass 2026-07-25 (handover readiness, Brian-authorized): every known defect across M2.1–M2.4 is
  now fixed or explicitly dispositioned, and the milestone record has been corrected to match observed
  reality. Three defects were found and fixed in this pass. **M2-R9 (P2)** — a remote migration against the
  real preview D1 had failed (Wrangler's remote path splits on `;` and cut the ownership triggers in half),
  been fixed by splitting them into `0003_ownership_triggers.sql`, and been recorded *nowhere*: not in this
  document, and contradicted outright by the M2.2–M2.4 handoff's "no remote database was touched". The
  technical half is resolved and independently re-verified; the governance half — whether that remote
  mutation was authorized at the time — is Brian's to confirm and cannot be established from the repository.
  **M2-R11 (P2)** — the guard test written to prevent a recurrence did not actually work: it counted
  leftover statements and allowed one, so a trigger beside a single `CREATE TABLE` would still have shipped.
  Found only by testing the guard against deliberately-bad SQL; now fixed and proven by six such tests.
  **M2-R10 (P3)** — the deployed Worker reported `version: 0.1.0-m1` while serving M2 code. Also in this
  pass: **M2-R4 (P1) resolved** (its premise had gone stale — the OAuth client had existed since
  2026-07-25; Brian chose preview as the staging target, recorded as M2-D15), and **M2-R7 (P2) reduced**
  from "has never contacted Google" to "the callback legs are unexercised", after a live read-only check
  confirmed `GET /api/v1/auth/start` returns a correct `302` to Google's real authorization endpoint with
  PKCE and a 256-bit state. Checks: format/lint/typecheck PASS, **462 tests** PASS, empty-DB migration PASS
  twice with idempotent re-apply, build PASS, `git diff --check` exit 0, secret scan clean. Remote preview
  D1 independently re-verified read-only: 10 tables / 3 triggers / 14 indexes / version 2 / **0 data rows**.
  Nothing was committed, deployed, or migrated in this pass.

Final sign-off, 2026-07-27: after the correction wave recorded above (M2-R12/13/14; M2-QA-01 through
  M2-QA-05; M2.5's adversarial review and M2-AR-01/02/03; M2-AR-01-FOLLOWUP; the M2-R3 restore
  rehearsal; M2-FQA-01 through M2-FQA-06; M2-FQA-RR-01 through M2-FQA-RR-03; M2-FQA-RR2-01;
  M2-FQA-RR3-01 — five consecutive independent Codex review/re-review rounds, each finding and closing
  real gaps in the previous round's own fix, on authorization, private-content write-scoping, and
  migration-delivery-safety axes — Codex's final re-review recommended acceptance with 0 open P0/P1/P2.
Claude status (final): Ready for PM/QA — all Codex findings across every round resolved and covered by
  regression tests. 552 tests passing, format/lint/typecheck/build all PASS, `git diff --check` exit 0.
  Nothing has been committed, deployed, or migrated remotely by any pass in this milestone.
Codex review (final): Complete. Recommends acceptance.
Open P0/P1: 0. Open P2/P3: M2-AR-02 (P3, admin task-move audit vocabulary, deferred to M4 by design),
  M2-AR-03 (P3, explicit exp/iss checks, deferred to M2-D11's JWKS follow-up), M2-R5 (P3, canonical-plan
  documentation drift, non-blocking).
Brian decision: **Accepted, 2026-07-27**, with M2-R7 explicitly carried forward rather than closed —
  Brian is holding the live Google OAuth callback/token-exchange verification, and applying migration
  `0004_identity_subject_pending.sql` to the real `dash2-preview` database, for a later work wave. This
  is a knowing, recorded deferral of one verification step, not a defect the milestone failed to catch:
  the code path is fully tested against real D1/KV with a synthetic provider, and the corrected schema
  is proven safe to apply (M2-FQA-RR2-01's upgrade-path verification) whenever that migration is
  authorized. `dash2-preview` remains at schema version 2 until that authorization is given; the Worker
  serving it must not be redeployed with the corrected identity-binding code until `0004` is applied
  first, per the ordering already recorded in M2-FQA-RR2-01.
Decision date: 2026-07-27
Notes: M2.2 delivered a central policy layer (6 pure modules) and 6 invariant-preserving services; no route
  handler makes an authorization decision. M2.3 delivered the full OAuth/session lifecycle behind an
  identity-provider seam — one-time hashed expiring state, opaque hashed sessions, 30-day sliding plus a
  90-day absolute ceiling (M2-D12, a bound M0 did not address), and immediate revocation via auth_version
  rechecked on every request. M2.4 delivered closed runtime validation and allowlist-constructed DTOs, and
  completed the administrative privacy boundary so an Admin can restore or purge a private task by opaque
  id while receiving no name, note, privacy flag, or history value. Tests went 135 → 446, all PASS, with
  the security-relevant ones running against real Miniflare KV and a real migrated D1 rather than mocks.
  Eight decisions were taken inside Claude's authority (M2-D7..M2-D14); the two most consequential for
  review are M2-D12 (absolute session ceiling) and M2-D14 (origin enforcement changes one M1 catch-all
  response from 404 to 403 on unsafe methods — deliberate, documented, and pinned by tests). Full detail,
  exact commands, and results: `.handoffs/M2-handoff.md`.

Claude self-review pass 2026-07-25 (Sonnet 5, deviation from the milestone's specified Opus 5 `xhigh`
  review, recorded per Brian's explicit choice): a first-pass read-through of M2.1–M2.4 in place of the
  not-yet-run M2.5, found and fixed 3 issues (M2-R12 P2, M2-R13/R14 P3). No P0/P1 found. Not a substitute
  for M2.5 or Codex QA.

Codex QA 2026-07-25 (whole-milestone, `5a00b54..78e1484` plus worktree): independent review found 5
  actionable defects — M2-QA-01 (P1, recycled Lists/ineligible-owner Lists remained reachable through
  ordinary authorization), M2-QA-02 (P1, KV's eventual consistency undermines the claimed one-time
  OAuth-state and immediate-logout guarantees), M2-QA-03 (P1, a non-atomic state-change-then-auth-version-
  bump could let restore resurrect a pre-recycle/disable session), M2-QA-04 (P2, no rate limit or redirect-
  path bound on unauthenticated sign-in initiation), M2-QA-05 (P2, the checked-in record contradicted
  itself on OAuth-client/staging/commit/deploy state). Gate recommendation: CHANGES REQUESTED.

Correction pass 2026-07-25 (Codex QA findings, Brian-instructed "review the codex results and fix it"):
  M2-QA-01, 03, 04, and 05 resolved in application code/tests/documentation. M2-QA-02 is decision-gated,
  not silently fixed: both structural remedies (a Durable Object, or moving session/state storage to D1)
  cross an approved boundary (Durable Objects are explicitly not-yet-approved infrastructure; D1 sessions
  would reopen M0-D12/M2-D2), so a decision brief was recorded (M2-QA-02-DECISION in the Decision Log)
  and the code's overstated "atomic"/"immediate" KV comments were corrected to describe the actual,
  accepted, bounded gap instead. 19 new regression tests added. Checks: format/lint/typecheck PASS,
  **483 tests** PASS (was 464), build PASS, `git diff --check` exit 0, secret scan clean. Nothing
  committed, deployed, or migrated in this pass. Full detail: `.handoffs/M2-handoff.md`.

M2-QA-02-DECISION resolved 2026-07-25: Brian approved Option A (accept the residual KV eventual-
  consistency race as a documented, bounded risk; no new infrastructure, no reopening M0-D12/M2-D2). All
  5 Codex QA findings are now dispositioned — 4 fixed in code, 1 resolved by risk acceptance. No further
  code change follows from this decision; the comment corrections were already made in the correction
  pass above. Codex `M2 Re-review` remains the next open step for all five findings.

Codex `M2 Re-review` 2026-07-25 of the QA correction wave: **CHANGES REQUESTED.** M2-QA-01, 03, and 05
  confirmed resolved. M2-QA-02 confirmed resolved by Brian's recorded risk acceptance. M2-QA-04 found
  **partially** resolved: the redirect-length bound was fine, but the rate limiter's design conflicted
  with a real Cloudflare KV limit (one write per second per key), so two ordinary rapid requests from one
  IP could make the second write throw and surface as a 500. Two new findings from the correction itself:
  **M2-RR-01 (P1)** — Claude's M2-QA-01 fix over-extended the recycled-owner-hides-the-List rule to
  merely *disabled* owners too, contradicting `AccountService.disable`'s own "keeps owning its Lists"
  contract and breaking a primary collaboration workflow (an admin disabling one user's login would have
  silently locked out that user's Editors/Viewers from an unrelated, non-recycled List). **M2-RR-02
  (P2)** — the M2-QA-02 code comments and one acceptance-criterion line still read as if the KV-consistency
  decision were pending after Brian had already approved it.

Correction pass 2026-07-25 (Codex re-review findings): **M2-RR-01 fixed** — both the service-layer and
  repository-layer checks narrowed from "owner not active" to "owner recycled" specifically; the
  regression test that had pinned the wrong (over-broad) behavior was corrected to prove the opposite —
  a disabled owner's existing Editors/Viewers keep their List, task, and listing access, while the owner
  themself still cannot authenticate (enforced at the session layer, not by hiding the List).
  **M2-QA-04's write-limit bug fixed** — `checkRateLimit` now fails open (treats the request as allowed)
  if the KV write itself throws, so a same-key write conflict cannot surface as an error to an ordinary
  caller; a regression test simulates a rejecting KV `put` and confirms the request is still allowed.
  **M2-RR-02 fixed** — the `oauth-state.ts`/`session.ts` comments and the OAuth-state acceptance criterion
  now state Brian's approval explicitly rather than "pending," and the criterion is qualified to what the
  single-location Miniflare tests actually establish. Checks: format/lint/typecheck PASS, **484 tests**
  PASS (was 483), build PASS, `git diff --check` exit 0. Nothing committed, deployed, or migrated in this
  pass. Full detail: `.handoffs/M2-handoff.md`.

Codex second narrow re-review 2026-07-25: **CHANGES REQUESTED.** M2-RR-01 and M2-RR-02 confirmed
  RESOLVED. M2-QA-04 found **PARTIALLY RESOLVED, NOT FIXED** by the fail-open change: removing the
  uncaught-throw bug was correct, but the same fail-open path means the nominal 20/60s bound is not
  actually enforced under concurrent traffic, and Claude's Risk Log had inaccurately called it "fully
  resolved" without a corresponding Brian-owned Decision Log entry for the fail-open choice. Open P0/P1:
  0. Required next action: either build a concurrency-safe limiter, or get Brian's explicit risk
  acceptance recorded properly.

M2.5 adversarial review 2026-07-25 (Claude Fable 5, Brian's explicit model override; the milestone
  specifies a separate Opus 5 `xhigh` context, so this is a recorded deviation, not a substitution).
  Fresh session — it did not implement M2.1–M2.4. **One P1 found and fixed: M2-AR-01.** M2 built three
  independent barriers around the *administrative* surface to keep an admin away from private tasks,
  private notes, and history values. The review found the barriers could be walked around instead of
  through: content visibility derives from `ownerUserId === actor.userId`, and both sides were writable.
  An Admin could transfer any List to themselves (`canTransferOwnership` never checked the destination),
  and an Admin **or any Editor** could move a task into a List they own — either way becoming the
  legitimate owner, after which the ordinary owner surface returns exactly what the boundary exists to
  withhold. Reproduced against real migrated D1 rather than by inspection: a temporary proof asserting
  the *bad* outcome passed pre-fix four ways. Fixed at the policy layer (`canAssignOwnershipTo`,
  `moveAcquiresOwnership`), so no service or route can miss it, with legitimate recovery and ordinary
  moves left intact. Checks: format/lint/typecheck PASS, **508 tests** PASS (was 484), build PASS,
  migrations idempotent PASS, `git diff --check` exit 0, secret scan clean. Nothing committed, deployed,
  or migrated. Two P3 observations recorded (M2-AR-02 unaudited admin task move, deferred to M4;
  M2-AR-03 implicit `exp`/`iss` in `verifyIdToken`, tied to M2-D11). **Open P0/P1: 0** — meaning 0 found
  by this pass, the same claim every prior pass made before an external check found something. This is
  the fourth worked example in this milestone of a barrier that looked complete from the inside: three
  Codex passes and a Sonnet self-review had all read this surface without finding M2-AR-01. Brian owes
  one decision: the move fix narrows §2's literal "edit rights on both Lists" wording, and Claude
  recommends keeping the refusal. Full detail: `.handoffs/M2-handoff.md`.

M2-QA-04-DECISION resolved 2026-07-25: Brian reviewed Codex's finding and approved accepting the
  concurrent-burst gap as sufficient for launch rather than redesigning the limiter. Risk Log wording
  corrected from "fully resolved" (Claude's own inaccurate claim, caught by Codex) to "resolved by
  accepted residual risk," and a new M2-QA-04-DECISION row records the approval. No further code change.
  All findings from both Codex re-review passes (M2-RR-01, M2-RR-02, M2-QA-04) are now dispositioned.
```
