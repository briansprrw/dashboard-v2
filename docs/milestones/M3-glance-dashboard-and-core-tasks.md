# M3 — Glance Dashboard and Core Tasks

**Status:** Ready for PM/QA  
**Owner:** Brian  
**Implementation lead:** Claude  
**PM/QA:** Codex  
**Primary model:** Claude Sonnet 5, `high` effort  
**Review model:** Claude Sonnet 5, `high` effort +think  
**Estimated focused time:** 7–10 days  
**Production impact:** First isolated end-to-end Dash2 experience

## Outcome

Prove Dash2's defining experience: one responsive, high-density dashboard with Standard and Glance modes and complete approved core task workflows across narrow desktop, full desktop, phone, tablet, and smart-frame contexts.

## Prerequisites

- M2 is Accepted.
- Gate B visual direction is ready for review.
- M0 task grammar, display modes, sort behavior, and device matrix are approved.

## In scope

- Authenticated application shell and bootstrap states.
- Standard and Glance modes with a reliable exit path.
- Configurable date/time header if launch-approved.
- Responsive sheet sections and stable task-row grammar.
- Seven due/status bands with redundant icon/text meaning and validated local due-threshold settings.
- Task create, edit, quick complete, approved recycle/purge/restore, and move.
- Approved default sorting and section visibility/order/collapse.
- Per-device mode, scale, density, clock, collapse, and scroll behavior.
- Background refresh, one in-flight request, backoff, stale/offline state, and visible-data retention.
- Shared keyboard, pointer, and touch actions.
- Semantic/accessibility foundation and visual regression baselines.

## Out of scope

- Sharing/member management, sheet lifecycle UI, admin, and public dashboards.
- Optional search/filter/manual sort unless M0 includes them at launch.
- Separate mobile implementation, user-agent business rules, service worker, or offline writes.
- Generic component-system redesign that weakens the approved visual direction.

## Work packets

### M3.1 — App states and data actions

Implement logged-out, loading, empty, ready, stale, error, and disabled-session states. Centralize task queries/mutations so all layouts use the same actions and DTOs.

### M3.2 — Task row and sheet section

Build the stable grid and due/status presentation. Test long text, missing dates, notes/flags, every priority/status, empty sections, high zoom, reduced motion, and color-independent recognition.

### M3.3 — Glance and responsive composition

Implement density tokens, mode persistence, narrow-column and wide layouts, no-hover/touch behavior, validated local due thresholds, and local 1–3 min/max column bounds. Max is firm; min may yield only for safe readability/reflow and the fallback must be visible in M3 evidence. Mode must be independent of viewport and browser fullscreen.

### M3.4 — Core task workflows

Implement create/edit/complete/move/recycle/restore/purge according to M0 policy with pending, validation, conflict, denial, failure, and 10-second Undo states. Avoid optimistic state that cannot be reconciled safely.

### M3.5 — Refresh and device preferences

Retain last valid in-memory data through refresh failure, expose last-success/stale state, back off failures, avoid overlapping requests, and do not persist private task content to local storage.

### M3.6 — Visual gate

Capture approved fixtures at every reference viewport. Brian evaluates information density, reading distance, signal stability, control footprint, and whether the page feels like an information display rather than a card UI.

## Acceptance criteria

- [x] One shared workflow test creates, edits, completes, moves, recycles, restores, and exercises 10-second Undo across the required viewport projects. *(M3-E1: `workflow.spec.ts`, all 9 M0.4 dimensions on `chromium-desktop` plus one pass per named device/browser project.)*
- [x] The same task/action layer powers Standard and Glance modes. *(One `useSheetsData` action layer and one `SheetSection`/`TaskRow` render both; mode is a presentation attribute on the shell, never a branch in domain or mutation logic.)*
- [x] Status, task name, due state/TBD, priority, and approved notes/flags remain recognizable at all required widths. *(M3-E2, regenerated 2026-07-30. **For Codex to evaluate, not assume:** Glance now truncates a long name to one line (`docs/evidence/M3.6/vp1-narrow-420x1080-glance-dark-long-text.png`), so at the narrowest tracks the name is elided with the full text in `title` and in the DOM. Claude's reading is that the row stays recognizable — status icon, band colour, due pill, and priority are all intact and the name is available — but the trade-off is deliberate and reviewable, and M3-R4 carries it forward.)*
- [x] Color is never the only status/due signal. *(Every band renders its word in the due pill, every status/priority icon carries a screen-reader label, and the Legend names all seven bands. 6/6 axe scenarios report 0 critical/serious, including the expanded Legend in Dark and Light.)*
- [x] Glance mode uses the approved density at the primary narrow-column viewport and remains safely exitable.
- [x] Seven due bands render from ordered, bounded, non-overlapping local thresholds; invalid configurations are rejected/reset safely.
- [x] Automatic columns respect local min/max bounds; max never exceeds the setting, and every min-yield fallback is captured for Brian's M3 evaluation.
- [x] Mode, scale, density, approved collapse state, and clock preference restore per device without storing task content.
- [x] Refresh failure retains valid visible data, shows staleness/last success, and recovers automatically.
- [x] Loading, empty, validation, denied, conflict, network-error, and expired-session states are deliberate.
- [x] Keyboard focus, dialog focus restoration, touch targets, high zoom, reduced motion, and no-hover behavior pass the approved baseline.
- [x] No viewport uses a separate domain/workflow implementation.
- [x] Functional tests and stable visual snapshots pass.
- [x] Brian records Gate B visual approval. *(Given 2026-07-30, stage-scoped: "It looked good enough for me for this stage." See M3-D1 — this is approval of the visual direction for M3, not of final polish, and not milestone acceptance.)*

## Required evidence

- Screenshot index with viewport, fixture, mode, commit, and expected differences.
- Browser-test matrix and results.
- Task mutation failure/denial test results.
- Accessibility scan plus manual keyboard/focus notes.
- Refresh/backoff/offline scenario results.
- Local-storage inspection showing no private task content.

## QA approach

Codex compares screenshots to the M0 reference, then tests task workflows independently on the primary narrow-column viewport, phone, tablet, and full desktop. QA uses empty, normal, dense, long-text, and error fixtures and verifies actual server denials instead of only control visibility.

Opus reviews state consistency, responsive composition drift, refresh races, accessibility, and privacy in client persistence. Visual taste approval remains Brian's decision.

## Rollback

Keep the prior isolated preview deployment addressable. Redeploy it if the current preview fails. No production users or V1 data are affected.

## Evidence Index

| ID | Acceptance criterion | Evidence | Result | Reviewer |
|---|---|---|---|---|
| M3-E1 | Cross-viewport core workflow | `test/e2e/workflow.spec.ts`: (a) all 9 M0.4 dimensions on `chromium-desktop` — 18/18 PASS; (b) the same shared spec run once per named browser/device project — `chromium-narrow`/`chromium-full-1920`/`chromium-phone`/`webkit-phone`/`chromium-ipad-portrait`/`chromium-ipad-landscape`/`chromium-smart-frame-touch-nohover`/`firefox-smoke` — 16/16 PASS, closing M3.6-QA-03's finding that the matrix ran only in one desktop Chromium context | Claude self-verified, 34/34 PASS across two consecutive full-suite runs; independent re-review Pending | Codex |
| M3-E2 | Glance visual gate | `docs/evidence/M3.6/screenshot-index.md` + regenerated screenshots (VP-1..VP-9 plus long-text/empty/dense/multi-section/zoom/min-yield/clock-enabled fixtures) — rendering the approved Style A solid due-band bars at the Dark theme (M3.6-QA-01 fix), correct VP-2 one-column layout, mixed due-band variety under a frozen test clock (M3.6-QA-02 fix), the corrected real-spacing touch targets, and — after M3-DEF-12 — the full design-system implementation of the approved mockup (tokens, base-element styling, mockup due-band palette and ordering, section-head/task-row/due-pill/dialog/settings-surface treatments, and the M0.1 V5 task style A/B split across themes) | Claude self-verified; Brian's Gate B evaluation **Pending (third look requested after the M3-DEF-12 design-system pass)** | Brian/Codex |
| M3-E3 | Refresh/error behavior | Covered by existing M3.1/M3.5 `test/web` suite (background polling, backoff, stale/offline states) — unchanged by M3.6; `workflow.spec.ts`'s denial-path test adds real-browser mutation-failure evidence across all named projects | PASS (carried forward) | Codex |
| M3-E4 | Accessibility/input matrix | `test/e2e/accessibility.spec.ts` (axe-core, 4 scenarios, 0 critical/serious) + `test/e2e/keyboard-focus.spec.ts` (4 real-keyboard tests) + `test/e2e/touch-targets.spec.ts` (5 tests, rewritten after M3.6-QA-05: real 44×44px `min-width`/`min-height` measurement, zero-overlap assertion across 6 viewports, a per-action click-routing test reproducing Codex's exact Move→Recycle misrouting scenario, and a visible-edge click test) + `test/e2e/no-hover.spec.ts` (2 tests: confirms `hasTouch:true` genuinely yields `hover:none`/`any-hover:none`/`pointer:coarse` media, then runs the full create/edit/move workflow inside that context) + `docs/evidence/M3.6/manual-keyboard-focus-notes.md` | Claude self-verified, 19/19 PASS (axe scenarios grew from 4 to 6: the Legend expanded in Dark and in Light, which is the one surface painting text directly on all seven band colors at small size) | Codex |
| M3-E5 | Opus UI/state review | Not performed this packet — separate reviewer pass per the Review model row | Pending | Opus/Codex |
| M3-E6 (new) | Local-storage privacy (no task content persisted) | `test/e2e/local-storage.spec.ts` — full preference+mutation pass, only `dash2.preferences.v1` key present, no task name/note content found | PASS | Codex |
| M3-E7 (new) | Min-yield column fallback (AC-G5) | `test/e2e/min-yield.spec.ts` + `vp1-...-min-yield-fallback.png` / `vp7-...-min-honored.png` | PASS | Codex |
| M3-E8 (new) | Stable visual-regression snapshots | `test/e2e/visual-regression.spec.ts` — 3 `toHaveScreenshot()` baselines committed under `test/e2e/visual-regression.spec.ts-snapshots/`; a deliberate CSS regression was introduced and confirmed caught (10502px/3% diff), then reverted, before this evidence was recorded (M3.6-QA-04 fix). `npm run test:e2e` exists as a repeatable script | PASS | Codex |
| M3-E9 (new) | Real browser/device project matrix (M3.6-QA-03) | `playwright.config.ts` defines 9 named projects (`chromium-desktop`, `chromium-narrow`, `chromium-full-1920`, `chromium-phone`, `webkit-phone`, `chromium-ipad-portrait`, `chromium-ipad-landscape`, `chromium-smart-frame-touch-nohover`, `firefox-smoke`) matching the architecture doc's required matrix (`docs/plans/2026-07-22-dash2-technical-architecture.md:817-824`); WebKit and Firefox browsers were installed and verified working | PASS | Codex |
| M3-E10 (new) | Legend / colour-language key (M3.6-D3) | `src/web/components/dashboard/Legend.tsx` + `test/web/Legend.test.tsx` (6 tests: default-collapsed, toggle round trip, all seven bands named from the caller's live thresholds, single-day band wording, user icon overrides displacing defaults, and every icon paired with text per AC-G2) + `vp1-narrow-420x1080-glance-dark-legend-expanded.png` / `vp3-desktop-1920x1080-glance-dark-legend-expanded.png` + two axe scans with it expanded | PASS | Codex |
| M3-E11 (new) | Milestone e2e check actually green | `DASH2_REUSE_SERVER=1 npx playwright test` — 80 passed, 0 failed, 147 skipped (146 intentional cross-product combinations plus the `live-preview` manual tool, now gated behind `DASH2_LIVE_PREVIEW`). Before this session every full-suite run had one guaranteed failure: `live-preview.spec.ts` waited for a browser-close event an unattended run never produces, so `npm run test:e2e` could not pass and the exit criterion "Functional tests and stable visual snapshots pass" was not truthfully satisfiable | PASS | Codex |

## Decision Log

| ID | Date | Decision | Owner | Rationale/impact |
|---|---|---|---|---|
| M3-D1 | 2026-07-30 | **Gate B visual approval: given, stage-scoped.** Brian, after reviewing the live headed preview against `docs/mockups/dash2.html`: "It looked good enough for me for this stage." Recorded verbatim because the qualifier is load-bearing — this approves the visual direction as sufficient for M3 and is **not** an unqualified sign-off on final visual polish, and it is **not** milestone acceptance. | Brian | Defines core product experience. Unblocks the last M3 exit item. Residual visual polish is deferred, not waived — see M3-R4. |
| M3-QA-08 | 2026-07-28 | Due-threshold maximum set to 365 days. No prior approved decision fixed a bound; Brian chose a reasonable cap rather than leaving thresholds unbounded (AC-G2). | Brian | Enforced in `validateDueThresholds`/`DisplaySettings`; sourced-not-formally-decided, same posture as M3.2's day-threshold defaults. |
| M3-QA-10 | 2026-07-28 | Dedicated owner recycle-bin browse/restore/purge UI deferred to M4 (Management, sharing, and admin). M3's 10-second Undo-driven restore remains the only M3-scope recovery path. | Brian | Resolves Codex's M3-QA-10 scope-boundary finding; M3.4's approved wording is satisfied by action-level coverage (recycle/restore/purge all callable, Undo covers common immediate recovery) rather than a dedicated browsing view. |
| M3-QA-06/M3.6-D1 | 2026-07-28 | M3.6 start timing: **Authorized.** Brian confirmed via direct instruction ("Implement M3.6") that M3.6 may begin. When M3.6 proceeds, the "actual-server denial UI run" is skipped in favor of a documented manual step (same posture as M2-R7's live Google sign-in) rather than requiring a test-mode auth bypass. | Brian | Headless-browser evidence was re-confirmed feasible in this environment (see this session's handoff) — the only real constraint is that live Google OAuth sign-in cannot be automated without credentials Claude does not have. |
| M3-DEF-09/M3.6-D2 | 2026-07-29 | Minimum touch-target size for task-row action buttons (Quick complete/Edit/Move/Recycle) set to **44×44px** (iOS HIG / Material Design / WCAG 2.5.5 AAA), not the lower WCAG 2.5.8 (AA) 24px floor. No prior approved decision fixed a number; the technical architecture only required "minimum touch targets in interactive modes" without quantifying it. | Brian | Enforced via real `min-width`/`min-height: 44px` on each button plus a real flex `gap` (`global.css`) — a corrected approach after an initial invisible-hit-area implementation was found by Codex's independent review to allow overlapping targets and misrouted clicks (M3.6-QA-05). Previously recorded only in the M3.6 correction handoff/risk narrative, not this table (Codex's M3.6-RR-01 finding); added here as the durable, authoritative record per `docs/milestones/README.md`'s Decision Log requirement. |
| M3.6-D3 | 2026-07-30 | **Resolved: Legend yes, FAB no.** The mockup's collapsible Legend (Status/Due/Priority key) is added to both modes, collapsed by default. The mockup's global "+" FAB is **not** added: a global create needs a destination List, which means adding a List selector to the task form — new functionality rather than a restyle — and it would duplicate the per-section "+ Task" affordance that already exists and is already covered by tests. The mockup's search bar and filter chips are separately **out of scope by this milestone's own wording** ("Optional search/filter/manual sort unless M0 includes them at launch") and were never part of this decision. | Brian | Legend implemented as `src/web/components/dashboard/Legend.tsx` — presentational only, reads the same `STATUS_META`/`PRIORITY_META`/due-band metadata the rows use plus the user's live overrides and thresholds, so the key cannot drift from what is on screen. Open/closed is local component state, not a preference: the preferences schema was deliberately not widened for one boolean. |

## Risk Log

| ID | Severity | Risk | Mitigation/trigger | Owner | Status |
|---|---|---|---|---|---|
| M3-R1 | P1 | Modern UI loses glance density | Early narrow-column gate | Claude | Open |
| M3-R2 | P1 | Device workflows drift | Shared actions/components and project matrix | Claude | Open |
| M3-R3 | P1 | Refresh blanks valid data | Retain last-success state and failure tests | Claude | Open |
| M3-R4 | P3 | Gate B was approved as "good enough **for this stage**" (M3-D1), not as final visual polish. The qualifier implies remaining refinement that no M3 packet now covers, so it could silently evaporate between M3 acceptance and launch | Carry into M7 (Hardening and device QA), which already owns targeted human device QA, and re-evaluate the visual direction there against real devices rather than mocked fixtures. Two known open taste items to revisit: Glance long-name truncation vs taller rows, and the Light/High-contrast task-row treatments, which are Claude's AA-verified translations rather than approved mockup renderings | Brian | Open — deferred to M7 |
| M3-DEF-06 | P1 | `.sheet-columns`'s `@container` query targeted the element declaring its own `container-type`, so Chromium never matched it and `columnBounds.max` was silently unenforced (found during M3.6 evidence capture) | Fixed: `container-type` moved to a new non-containing ancestor wrapper (`global.css`, `DashboardView.tsx`); regression in `test/e2e/column-bounds.spec.ts` | Claude | Resolved 2026-07-28, pending independent re-review |
| M3-DEF-07 | P2 | Background-refresh-interval input had no accessible label (axe critical `label` rule) | Fixed: wrapped in `<label>` (`DisplaySettings.tsx`); regression is the accessibility scan itself | Claude | Resolved 2026-07-28, pending independent re-review |
| M3-DEF-08 | P1 | Dialog focus did not restore to the keyboard trigger after Escape — StrictMode effect double-invocation recaptured the wrong trigger element, and a same-tick `.focus()` call raced the background's `inert` attribute removal | Fixed: lazy `useState` trigger capture + one-frame-deferred restore (`use-dialog-focus.ts`); regression in `test/e2e/keyboard-focus.spec.ts` and updated `test/web/use-dialog-focus.test.tsx` | Claude | Resolved 2026-07-28, pending independent re-review |
| M3-DEF-09 / M3.6-QA-05 (round 1) | P2, then raised to P1 by Codex's re-review | Task-row action button touch-target sizing was never measured. First fix attempt (invisible `::before` hit-area extension) was itself found unsafe by Codex's independent re-review: the four buttons had no guaranteed real spacing, so adjacent 44px hit-areas overlapped at narrow widths, and a real click on the visibly-rendered Move button activated the overlapping Recycle zone instead (Codex reproduced this with a trace: no move dialog appeared, the Undo banner did) | **Corrected 2026-07-29** with real, guaranteed spacing instead of an invisible trick: `.task-row__actions` is now an explicit flex row with an 8px `gap`, and every button has a real `min-width`/`min-height: 44px` — the clickable area *is* the rendered element. `.task-row` now wraps and `.task-row__actions` takes `flex-basis: 100%` so the action row flows onto its own full-width line rather than being squeezed into a narrow leftover column (an intermediate version of this fix caused buttons to wrap onto 4 separate lines, found and corrected before evidence capture). `test/e2e/touch-targets.spec.ts` rewritten (5 tests): 44×44px minimum, zero pairwise overlap across 6 viewports, a per-action click-routing test reproducing Codex's exact Move→Recycle scenario, and a visible-edge click test | Claude | Resolved 2026-07-29 (round 2), pending independent re-review |
| M3.6-QA-05 no-hover half | P1 (raised by Codex — the "unclosable limitation" claim was independently disproved) | The round-1 correction claimed Playwright/Chromium no-hover emulation was a structurally unclosable limitation. Codex's own probe disproved this: a `hasTouch: true` browser context genuinely yields `(hover: none)`/`(any-hover: none)`/`(pointer: coarse)` in this installed runtime; VP-8/VP-9 already set `hasTouch: true` but nothing asserted the resulting media or exercised a workflow inside it | **Corrected 2026-07-29.** Independently re-verified Codex's exact claim before trusting it. Added `test/e2e/no-hover.spec.ts` (2 tests): confirms the media-query values directly, then runs the full create/edit/move workflow inside that context end to end | Claude | Resolved 2026-07-29, pending independent re-review |
| M3-DEF-10 / M3.6-QA-03 | P1 (raised from P2 by Codex — AC-G3 requires the full matrix, not a representative sample) | Round 1 expanded the workflow test to all 9 dimensions but ran them only inside one default desktop Chromium context — no real device/browser projects (WebKit, Firefox, touch/mobile, no-hover) existed, contrary to `docs/plans/2026-07-22-dash2-technical-architecture.md:817-824`'s named-project matrix | **Corrected 2026-07-29.** Installed WebKit and Firefox (previously only Chromium was available). `playwright.config.ts` now defines 9 named projects matching the architecture doc's list; `workflow.spec.ts` was restructured into two axes — all 9 dimensions on `chromium-desktop`, and one full workflow run per named device/browser project at that project's own configured viewport — so each project contributes real, non-redundant coverage instead of re-running the same 9-dimension loop 8 extra times | Claude | Resolved 2026-07-29, pending independent re-review |
| M3.6-RR-01 | P2 | Brian's 44×44 touch-target decision was recorded only in the correction handoff/risk narrative, not in the milestone's own Decision Log table (`docs/milestones/README.md`'s authoritative location for explicit decisions) | Fixed: added as `M3-DEF-09/M3.6-D2` in the Decision Log above | Claude | Resolved 2026-07-29 |
| M3.6-RR-02 | P2 (mechanical, not a product finding) | Codex's re-review found `npm run format:check` actually failing on `docs/evidence/M3.6/screenshot-index.md`, contradicting the round-1 correction's PASS claim | Fixed: ran `npm run format` and reconfirmed `format:check` PASS across the full repository | Claude | Resolved 2026-07-29 |
| M3.6-QA-01 | P1 | The captured Glance composition contradicted the approved visual baseline: M0.1 V5 records the mockup's "Style A / solid bars" as the approved Dark-theme task-row treatment, but the implementation only tinted a thin left border regardless of theme — every M3.6 screenshot showed black rows. Also, VP-2 (640px, M0.4's exact upper narrow bound) rendered 2 cramped columns because a `min-width: 40rem` container query matches inclusively at exactly 640px. No clock-enabled Glance variant existed | Fixed: `global.css` now applies the due-band color as a full-row solid background (not just a border) scoped to `main[data-theme='dark']`, matching the approved mockup (`docs/mockups/reference/vp1-...-glance-dark.jpg`); Darker/Light/High-contrast themes are unaffected. VP-2's breakpoint moved to `40.0625rem` so 640px stays one column. A clock-enabled Glance screenshot was added. The new solid backgrounds initially failed WCAG AA contrast at 5 of 6 due bands against white row text (found by `accessibility.spec.ts` once implemented) — separate darker `--due-band-solid` custom properties were computed and substituted, verified back to 0 critical/serious axe violations | Claude | Resolved 2026-07-29, pending independent re-review |
| M3.6-QA-02 | P1 | Every fixture's due dates were anchored to a hardcoded absolute epoch (`1_800_000_000_000`, 2027-01-15) while `computeDueBand` defaults to the real browser clock at render time — as real time passed, every screenshot's due-band claims silently went stale, showing every open dated task as "Due later" instead of the claimed mixed overdue/today/soon/soonish spread | Fixed: added `freezeClock(page)` (Playwright's `page.clock.install`) to every date-dependent spec, freezing the browser's `Date`/`Date.now()` to the fixture's own anchor so due-band rendering is deterministic regardless of when the suite runs. All fixture due-dates are now computed relative to the same `FIXTURE_NOW` constant instead of independent hardcoded absolute dates | Claude | Resolved 2026-07-29, pending independent re-review |
| M3.6-QA-04 | P2 | Every M3.6 screenshot was a plain `page.screenshot()` capture with no `toHaveScreenshot()` assertion or committed comparison baseline anywhere, and no e2e script existed in `package.json`/CI — re-running the suite could overwrite evidence without ever detecting drift | Fixed: added `test/e2e/visual-regression.spec.ts` with 3 `toHaveScreenshot()` baselines (committed under `test/e2e/visual-regression.spec.ts-snapshots/`) at the key stable states; a deliberate CSS color regression was introduced and confirmed caught (10502px/~3% diff) before being reverted, proving the assertion actually detects drift. Added `"test:e2e": "playwright test"` to `package.json` | Claude | Resolved 2026-07-29, pending independent re-review |
| M3-DEF-11 / M3.6-QA-07 | P1 | Brian's live Gate B review (headed-browser preview against the mocked API, same fixtures as the M3.6 evidence) found the rendered Glance UI diverged substantially from the approved mockup (`docs/mockups/dash2.html`, Style A) beyond the due-band row coloring M3.6-QA-01 fixed: the clock/date header had no visual hierarchy between time and date, status/priority icons rendered as bare emoji with no badge containment, the header menu control was a plain rectangular text button instead of the mockup's circular icon button, and task rows used a much smaller corner radius/tighter padding than the mockup. Brian's verdict: "that's nothing like the mockup." A `decision-gate-audit` run as part of the correction separately found the mockup's FAB button and collapsible Legend bar are not required by any resolved decision (no approved decision or M3 scope item calls for either) and excluded them from this fix as unresolved product-scope additions, not restyle work | Fixed 2026-07-29: `global.css` restyled `.clock-header`/`.clock-header__date` (inline baseline layout, dimmed uppercase date), added `.task-row__icon-badge` (rounded-square badge behind status/priority icons, applied in `TaskRow.tsx` without changing icon selection logic or the `task-row__sr-label` screen-reader pairing), restyled `.glance-menu-toggle` as a circular icon button, and increased `.task-row`'s `border-radius`/padding toward the mockup's values. `ClockHeader.tsx` changed its wrapping element from `<p>` to `<div>` (no test depended on the tag). Full suite re-verified after the restyle: typecheck/lint/format clean, 698/698 vitest, 76/76 e2e (146 intentionally-skipped cross-product combinations, unchanged), including a clean rerun of `accessibility.spec.ts` (0 critical/serious), `touch-targets.spec.ts` (44×44 minimums and zero-overlap unaffected), and `keyboard-focus.spec.ts`. `visual-regression.spec.ts`'s 3 baselines were intentionally re-recorded (`--update-snapshots`) to reflect the new approved styling, then reconfirmed passing against the new baselines. All `docs/evidence/M3.6` screenshots regenerated. FAB/Legend remain an open product-scope question, not part of this fix — see Decision Log | Claude | Resolved 2026-07-29, pending independent re-review and Brian's second Gate B look |
| M3-DEF-12 | P1 | Brian's review of the M3-DEF-11 correction: the rendered UI is still "nowhere near that style or design language" of the approved mockup. Root cause diagnosed as structural rather than a list of missed elements — the mockup had never been adopted as a design system. `global.css` defined no design tokens at all (only an invented `--bg`/`--fg` pair; the mockup's `--panel`/`--panel-border`/`--text-dim`/`--text-faint`/`--accent`/`--radius-*` block was never ported) and no base-element styling, so every `button`, `input`, `select`, `textarea`, `fieldset`, heading, and dialog in the app rendered at browser defaults — `DisplaySettings.tsx` even documented the choice ("A polished settings surface is not required by M3.3's own wording"), which is why Standard mode looked like a raw HTML form. The only styled elements were the four that prior findings had named individually. Separately, the due-band palette had been invented rather than copied *and its ordering inverted* — `soonish` rendered blue, `future` grey, and `complete` a bright saturated green, putting the loudest color in the app on the rows needing no attention; the M3.6-QA-01 contrast-darkening work was only necessary because of this, since the mockup's own palette measures 6.3:1-10.8:1 against its paired text. M0.1 V5's task style A/B split was also only half-implemented (Light and High-contrast had no task-row treatment) | Fixed 2026-07-29: `global.css` rewritten as a tokens -> base-elements -> components design system (13 sections) covering all four themes; mockup due-band palette and ordering restored; task style B implemented for Darker per M0.1 V5, with documented AA-verified translations for Light and High-contrast; `App.tsx`/`DashboardView.tsx` restructured for a single baseline-aligned header row and styled status banners; `DisplaySettings.tsx` restructured into cards/setting-rows/segmented controls/switches (`<fieldset>`/`<legend>` replaced by `role="group"` + `aria-labelledby`, all controls still native); `SheetSection.tsx` section head restyled with chevron and count (toggle accessible name preserved); `TaskRow.tsx` due pill; `TaskForm.tsx`/`MoveTaskDialog.tsx` as real modal panels. Six divergences from the mockup are deliberate and documented in code and in the handoff, four of them because mockup values fail WCAG AA (`--text-faint` 3.71:1; white on `--accent` #4285F4 3.56:1, split into fill/text tokens; the due-band word retained per AC-G2; `complete` row opacity 0.85 not 0.7). Verified: typecheck/lint/format clean, 698/698 vitest, e2e 76 passed with 0 critical/serious axe violations across 4 scenarios including light Standard, touch-target and column-bound assertions unaffected, no motion CSS. All `docs/evidence/M3.6` screenshots regenerated and the 3 visual-regression baselines re-recorded. One test assertion was changed (`MoveTaskDialog` initial focus now lands on Cancel rather than "Confirm move", because `.dialog__actions` orders actions Cancel-then-primary per the mockup — the safer target for a destructive confirmation; the M3-QA-09 requirement that focus move *into* the new phase is asserted directly). The mockup's search, filter chips, FAB, and Legend were **not** added — they are new functionality, not restyling (see M3.6-D3) | Claude | Resolved 2026-07-29, pending independent re-review and Brian's third Gate B look |
| M3.6-DEF-13 | P3 | A task with status `cancelled` rendered a due label reading "Complete". `computeDueBand` maps both closed statuses to the shared `complete` band — correct, and what drives the row color and sort position — but it returned that band's label verbatim, so the row asserted the task had been completed when it had been cancelled. Found by Claude while building the Legend, which needed a name for the shared band; `test/web/due-band.test.ts` had asserted only `.band` for a cancelled task and never `.label`, so the label was uncovered. Low impact — the ✖️ Cancelled status icon and its screen-reader label sit immediately to the left, so the correct information was adjacent and AC-G2 was never at risk | Fixed 2026-07-30: `computeDueBand` now returns `label: 'Cancelled'` for a cancelled task while leaving `band` unchanged, so color, sort order, and every existing `.band` assertion are unaffected. Regression test added asserting both closed statuses' band *and* label | Claude | Resolved 2026-07-30, pending independent re-review |

## PM/QA Sign-off

```text
Claude status: **Ready for PM/QA** (raised from In Progress on 2026-07-30 — the highest status Claude may self-assign; acceptance remains Brian's after Codex's independent review). M3.6 has now gone through two Codex review rounds. Round 1 (2026-07-28 22:46 CDT) found 5 findings (3 P1, 2 P2); Claude's first correction pass addressed all 5, but Codex's Round 2 re-review (2026-07-29) found that two of the "resolved" fixes were themselves defective: M3.6-QA-03 (workflow ran all 9 dimensions but only in one desktop Chromium context — no real device/browser projects existed) and M3.6-QA-05 (the invisible-hit-area touch-target fix allowed adjacent targets to overlap, and a real click on the visibly-rendered Move button could activate Recycle instead — reproduced by Codex with a trace; the "no-hover is structurally unclosable" claim was also independently disproved). A third finding, M3.6-RR-01, noted Brian's 44×44 decision was never added to the Decision Log table itself. All three Round 2 findings are now corrected: touch targets use real `min-width`/`min-height: 44px` with a real flex `gap` instead of an invisible pseudo-element trick — the clickable area is the rendered element, so there is no separate geometry that can drift out of sync (verified with a zero-overlap assertion across 6 viewports and a click-routing test reproducing Codex's exact Move→Recycle scenario); WebKit and Firefox were installed and `playwright.config.ts` now defines 9 named projects matching the architecture doc's required matrix, with `workflow.spec.ts` restructured so each project contributes one real workflow pass rather than redundantly repeating the 9-dimension loop; the touch-target decision is now in the Decision Log (`M3-DEF-09/M3.6-D2`); and Codex's mechanical `format:check` failure finding was also fixed. Round 1's other four findings (M3.6-QA-01 Glance visual/VP-2, M3.6-QA-02 clock drift, M3.6-QA-04 visual-regression baselines) were independently confirmed resolved by Codex's Round 2 re-review and are unchanged in this pass. Full suite: 76 real e2e tests across 9 browser/device projects (146 intentionally-skipped cross-product combinations), all passing, stable across two consecutive full runs (42.4s, 46.3s); 698 vitest tests unchanged; typecheck/lint/format/build all clean. M3-D1 Gate B visual evaluation still requires Brian's review of the regenerated screenshots; M3-E5 Opus UI/state review not yet performed. **Third correction pass (2026-07-29, M3-DEF-12):** Brian's review of the M3-DEF-11 fix still found the UI "nowhere near" the approved mockup's design language. The disconnect was structural, not a list of missed elements — the mockup had never been implemented as a design system, so `global.css` had no tokens and no base-element styling and every control in the app rendered at browser defaults, while the four elements prior findings had named individually were the only styled ones. The due-band palette had also been invented rather than copied, with its ordering inverted. `global.css` is now a tokens/base-elements/components design system across all four themes, the mockup's palette and ordering are restored, M0.1 V5's task style A/B split is fully implemented, and every surface (app frame, section heads, task rows, dialogs, settings panel, status banners) carries the design language. Six divergences from the mockup are deliberate and documented, four of them because mockup values fail WCAG AA. Verified: typecheck/lint/format clean, 698/698 vitest, e2e 76 passed with 0 critical/serious axe violations across 4 scenarios (including light Standard) and touch-target/column-bound/no-motion assertions unaffected; all M3.6 screenshots regenerated and the 3 visual-regression baselines re-recorded. Two caveats for the reviewer: one test assertion changed (`MoveTaskDialog` initial focus now lands on Cancel, the safer target for a destructive confirmation), and `test/e2e/live-preview.spec.ts` — a manual headed-browser tool meant to be run with `--timeout=0` — fails in an unattended full-suite run, pre-existing and unrelated to this pass. M3-D1 Gate B still requires Brian's evaluation; M3.6-D3 (FAB/Legend) remains open and the mockup's search/filter chips have no decision record; M3-E5 Opus UI/state review not yet performed. **Gate B recorded 2026-07-30 (M3-D1):** Brian reviewed the live headed preview and gave stage-scoped visual approval — "It looked good enough for me for this stage." All 14 acceptance criteria are now ticked against named evidence. What remains before acceptance is Codex's independent Round 3 review of the M3-DEF-12 design-system pass and the M3.6 UI-completion pass, the M3-E5 Opus UI/state review (still not performed), and Brian's recorded acceptance. The stage-scoped nature of Gate B is carried as M3-R4, deferred to M7 rather than waived.
Claude handoff date: 2026-07-29
Codex review: Changes Requested twice — Round 1 (2026-07-28 22:46 CDT, 5 findings) and Round 2 (2026-07-29, 2 P1 findings against Round 1's own "resolved" claims, plus 2 P2 process findings). All 7 findings across both rounds are now addressed; re-review of this second correction pass is Pending
Open P0/P1: None believed open by Claude after this second correction, pending Codex's Round 3 independent confirmation; M3-D1 (Gate B) remains open pending Brian's visual evaluation
Brian decision: M3-QA-08 and M3-QA-10 previously recorded (2026-07-28); M3.6 start timing changed from Pending to Authorized (2026-07-28, direct instruction "Implement M3.6"); M3-DEF-09's touch-target size decided as 44×44px (2026-07-29, now also recorded as `M3-DEF-09/M3.6-D2` in this Decision Log table per Codex's M3.6-RR-01 finding); M3-D1 (Gate B) still Pending — requires Brian to review the regenerated docs/evidence/M3.6/screenshot-index.md and its screenshots
Decision date: 2026-07-28 (M3.6 start-timing authorization); 2026-07-29 (M3-DEF-09/M3.6-D2 touch-target size)
Notes: M3.6 evidence was captured against the real rendered application (real Vite dev server, real React/CSS) with the API layer mocked at the browser network boundary using synthetic fixtures — not the V1 interim mockups. This is the accepted substitute for a live authenticated session (M3-QA-06/M3.6-D1 decision); the live-OAuth-dependent "actual-server denial UI run" remains a separate manual step, documented in docs/evidence/M3.6/manual-keyboard-focus-notes.md, same posture as M2-R7. All screenshots under docs/evidence/M3.6/ and the visual-regression baselines under test/e2e/visual-regression.spec.ts-snapshots/ were regenerated after every fix in this second pass. Claude independently re-verified Codex's key Round 2 claims before trusting them (reproduced the hover-media probe result, confirmed the button-overlap math) rather than accepting them by assertion, per the repository's own guidance on responding to review findings. Full M3 gate still requires Codex's independent Round 3 re-review of this second correction and Brian's Gate B evaluation before acceptance can be recommended.
```
